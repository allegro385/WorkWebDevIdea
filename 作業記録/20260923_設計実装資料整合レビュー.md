# 2026-09-23 設計・実装・資料の整合レビュー

## 対象と方法

GitHub `main` の `b522f658ad327609b907b4287c06872324320013` を新規クローンした状態で、要件・画面・構成・DB・運用・未決事項・Common/Portal詳細設計・テンプレート方針、Portal/Common/Templateの実装とREADME、初期SQL、HTMLモックのREADMEを静的に照合した。実DB・IIS・SMTP・ブラウザーでの動作検証は行っていない。以下は確認できた差分であり、全機能に矛盾がないことの保証ではない。

## 再クローン直後に確認した差分（以下は修正前の記録）

1. **テンプレートの実行成功ログが出力完了より先に記録される。** Common詳細設計の第7節は`WEB_EXECUTE`を業務処理の完了箇所で1回記録し、結果未確定なら成功を記録しないとする。`ToolController.Execute`は`WEB_EXECUTE/SUCCESS`を書いた後に`WriteTsvAsync`と`BuildFileName`を呼ぶ。出力で例外・キャンセルが起きると、利用者への出力が失敗しても成功ログが残る。`CalculateAsync`等の例外・キャンセルは`FailureViewAsync`を経由せず、失敗ログも記録されない。対象：`設計書/10_Common詳細設計.md:252`、`SalesSupport/src/Template/SalesSupport.Template.Web/Controllers/ToolController.cs:59-62`、同`77`。修正方針は、出力を確定してから成功を記録し、例外・キャンセル経路の記録単位を設計に合わせて整理すること。
2. **テンプレートの入口ログが正常応答より先に記録される。** Common詳細設計の第7節は正常な入口画面応答だけを`WEB_OPEN`の対象とする。`ToolController.Index`は画面モデル生成前に成功を記録する。`BuildViewAsync`でアップロード条件を取得できない場合は例外を返すため、画面失敗時にも成功ログが残る。対象：`設計書/10_Common詳細設計.md:251`、`SalesSupport/src/Template/SalesSupport.Template.Web/Controllers/ToolController.cs:30`、同`35`、同`93`、`SalesSupport/src/Template/SalesSupport.Template.Web/Services/EstimateService.cs:74-79`。
3. **確認データの保持条件は未確定と記載したまま暫定値で実装されている。** Portal詳細設計第6・14節と検討事項第1節は、保持期限・総保持量・上限時の扱いを実装前に確定すると記す。一方、`ConfirmationStore`は30分・同時200件で稼働する実装で、件数以外のメモリー総量上限はない。READMEは暫定であると明示している。仕様判断を要する差分であり、30分・200件を恒久仕様と読み替えない。対象：`設計書/11_Portal詳細設計.md:146`、同`256`、`設計書/06_検討事項.md:26-31`、`SalesSupport/src/Portal/SalesSupport.Portal.Web/Services/ConfirmationStore.cs:31-35`、Portal README `:116`。
4. **テンプレート生成の命名規則が未決事項にも残る。** 検討事項第12節は「生成スクリプト作成前」にToolIdからのC#識別子・ソリューション・フォルダーの規則と生成先を決めるとしているが、翌日のテンプレート方針第3節と生成スクリプトREADMEで規則を定義し、スクリプトも実装済み。未決事項の項目を、残るサンプル専用の判断と確定済みの生成規則に分けて更新する必要がある。対象：`設計書/06_検討事項.md:155-160`、`設計書/12_サンプル・テンプレートツール設計方針.md:3`、同`37-46`、`SalesSupport/scripts/README.md:29-54`。
5. **テンプレートのソリューション構成の文言が実体と合わない。** テンプレート方針第1節は「各ソリューションには対象ツールとCommonだけを登録」と書くが、`template.slnx`にはテストプロジェクトも登録されている。同資料第3節はテストプロジェクトの生成を明記する。第1節の「だけ」を実際の3プロジェクト構成へ更新する必要がある。対象：`設計書/12_サンプル・テンプレートツール設計方針.md:13`、同`45`、`SalesSupport/template.slnx:3-11`。
6. **画面設計に古いお気に入りの実装段階が残る。** 画面設計第5節の2026-09-16の注記は「認証未実装」「お気に入り登録・解除は無効」「保存未実装」とするが、Portal READMEはP003/P008の登録・解除を実装済みとしている。日付付きの履歴情報ではあるが、現行仕様と同じ節にあるため現在の動作を誤読しやすい。履歴へ移すか「過去の画面確認段階」と明確化する。対象：`設計書/02_画面・機能設計.md:236-241`、Portal README `:16`。

## 未完了として明示されている項目

- 要件・開発標準は標準テンプレートとサンプルツールの提供を求めるが、方針書はサンプルツール、機能追加手順書、開発専用SQLを未実装と明記する。現時点の実装漏れであり、仕様矛盾と混同しない。対象：`設計書/01_要件定義.md:151`、`設計書/03_システム構成・開発標準.md:45`、`設計書/12_サンプル・テンプレートツール設計方針.md:3`。
- 既存DB移行、実DBでの行ロック・制約、実SMTP、IIS、複数アプリ間共有Cookieは別途環境での検証が必要。設計・READMEも未検証と明記している。

## 2026-09-23の対応結果

- 1、2：テンプレートの入口ログを正常な画面応答の後に記録する結果フィルターへ移した。実行ログは出力準備の成否が確定してから記録し、例外・キャンセル時は失敗を一度記録する。関連テストを追加した。
- 3：ユーザーが発行から30分・全用途合計200件を正式仕様として確定した。上限到達時は新規発行を拒否し、期限切れ・消費後に再試行する。バイト数の独立上限は設けない。Portal詳細設計、画面設計、README、コードコメントを更新し、検討事項から削除した。
- 4：検討事項から確定済みの生成命名規則を除き、サンプルツール固有の未決事項だけを残した。
- 5：テンプレート方針を、対象ツール・Common・対象テストの3プロジェクト構成に修正した。
- 6：画面設計の2026-09-16注記を現行実装に適用しない過去の記録として明記した。
- 追加：`ConfirmationStore`の本人以外の要求時にデータを維持する実装に合わせ、誤ったコメントとテスト説明を直した。

`dotnet test SalesSupport/template.slnx --no-restore`は17件、`dotnet test SalesSupport/SalesSupport.Portal.slnx`は58件成功。実DB・IIS・SMTP・ブラウザーの結合検証は実施していない。
