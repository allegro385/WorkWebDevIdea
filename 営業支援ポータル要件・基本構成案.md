# 営業支援ポータル 要件・基本構成案

## 1. システム概要

### 1.1 目的

- 既存の営業支援用デスクトップツール群と、新たに開発するWebツールを一つのポータルへ集約する。
- 利用者がツールの実装方式を意識せず、同じサイトから必要なツールを利用できるようにする。
- 各ツールを独立したソリューションとして開発し、複数開発者による並行開発・保守を可能にする。
- 認証、ユーザー管理、ツール管理、お知らせ、通知、ログ、共通UI等はポータル側へ集約する。

### 1.2 想定利用規模

| 項目 | 想定 |
| --- | --- |
| 登録ユーザー数 | 約200名 |
| 同時利用ユーザー数 | 約5名 |
| 初期ツール数 | 約20ツール |
| ツール種別 | Webツール / デスクトップツール |

### 1.3 初期対象外

- ツール単位の利用者権限管理
- ツール一覧のキーワード検索
- 利用状況集計のシステム内グラフ表示
- ポータル管理画面からのWebアプリケーション本体の配備
- 問い合わせメールへの一時添付を除く、一般ユーザー向けファイルアップロードおよびファイルの恒久保存
- CI/CD
- ポータルおよび各ツールへのバックアップ機能実装
- 計算結果および帳票データの恒久保存
- 厳密なレスポンスタイムおよび可用性SLAの数値化

---

# 2. 機能要件

本章では、利用者・管理者・システムから見た動作を定義する。データ構造、API、保存先等の実装方式は第5章以降で定義する。

## 2.1 一般ユーザー向け機能

### 認証・利用制御

- 認証済みユーザーのみポータルを利用できる。
- ユーザーを一般ユーザーとシステム管理者に区分する。
- システム管理者のみ管理機能を利用できる。
- 無効化されたユーザーはポータルを利用できない。
- 非公開以外のツール情報は、すべての有効なユーザーが参照できる。
- ツールの起動・取得可否は、ツールの状態に応じて制御する。

### ポータルトップ

- システムのお知らせを表示する。
- お知らせの下に「ツール一覧」「サポート」「管理画面」の3列を表示する。
- ツール一覧には「全ツール」「お気に入り」を表示する。
- サポートには「個人設定」「利用マニュアル・FAQ」「問い合わせ・ご意見」を表示する。
- 管理画面には管理機能への入口を表示し、システム管理者にのみ表示する。

### ヘッダー・ユーザーメニュー

- ログイン中のユーザー情報を表示する。
- 認証方式に応じてログアウト等の操作を表示する。

### ツール一覧・お気に入り

- Webツールとデスクトップツールを同じ一覧に表示する。
- 一覧上では、Webツールとデスクトップツールの違いを表示しない。
- 全ツール画面とお気に入りツール画面は、同一の画面構成および操作方法とする。
- 各ツールを帯形式の一行で表示し、「お気に入り、大分類、ツール名、ツール概要、バージョン、更新日、状態、詳細への入口」を配置する。
- 任意のツールをお気に入りへ登録・解除できる。
- ツール一覧からお気に入りツールページへ移動できる。
- お気に入りツールページには、ログイン中のユーザーがお気に入り登録したツールのみを表示する。
- 非公開のツールは一般ユーザーへ表示しない。
- 使用可能、メンテナンス中、利用停止のツールは詳細画面を表示できる。

### ツール詳細

- 一覧の一行に相当するツール概要を表示する。
- ツール固有のお知らせとして、障害情報やメンテナンス予定等を表示する。
- 最新バージョンと過去のバージョン更新情報を表示する。
- ツールマスタに登録された利用者向け備考を表示する。
- ツールの利用マニュアルをダウンロードできる。
- 登録されている提供内容に応じて、Webページへのリンク、デスクトップアプリケーションのダウンロード、データリンク等を表示する。
- デスクトップツールをWeb化する場合は、当面は既存ツールの種別を変更せず、新しいWebツールとして別のToolIdで登録する。
- メンテナンス中または利用停止の場合も、お知らせ、バージョン更新情報およびマニュアルを参照できる。
- メンテナンス中または利用停止の場合は、ツールの起動、アプリケーションのダウンロードおよびデータリンクの利用を無効にする。

### お知らせ・メール通知

- 公開中のシステム全体のお知らせをポータルトップに表示する。
- 公開中のツール固有の障害、バグ、メンテナンス等のお知らせを、対象ツールを参照できる全ユーザーのツール詳細に表示する。
- システムのお知らせメールは、管理者が明示的に送信した場合に、通知を有効にしているユーザーへ送信する。
- ツールのお知らせメールは、管理者が明示的に送信した場合に、対象ツールをお気に入り登録し、通知を有効にしているユーザーへ送信する。
- バージョン更新情報の登録では、お知らせの自動登録およびメールの自動送信を行わない。

### 個人設定

- システムのお知らせメールを受信するか設定できる。
- お気に入りツールのお知らせメールを受信するか設定できる。
- 初期開発では利用者ごとの画面テーマ設定を設けない。
- 画面は社内システム統一テーマを使用し、将来のダークモード等は拡張想定とする。

### サポート

- 「利用マニュアル・FAQ」画面で、ポータルの利用マニュアルと共通FAQをまとめて参照できる。
- 利用マニュアルは、ポータルサイトの操作方法を記載したPDFへのリンクとして表示する。
- FAQは、No、カテゴリ、質問、回答を表示し、質問と回答を二段で構成する。
- FAQはカテゴリの表示順、カテゴリ内のFAQ表示順で表示する。
- 公開中のFAQのみ表示する。
- 各ツール固有のマニュアルはツール詳細から参照する。

### 問い合わせ・ご意見

- カテゴリ、対象、内容、任意の添付ファイルを入力して送信できる。
- カテゴリは「質問」「要望」「意見」「不具合報告」「その他」から選択する。
- 対象は「システム」「非公開を除く各ツール」「その他」から選択する。
- 内容は必須入力とし、最大2,000文字とする。
- 添付ファイルは任意の一ファイルとし、最大10MBとする。
- 添付できるファイルはPDF、Excel、Word、CSV、テキスト、PNG、JPEGに限定する。
- 送信者名およびメールアドレスはログインユーザーから取得し、入力欄は設けない。
- 送信ボタンの二重押下を防止する。
- 送信後に問い合わせIDを表示する。
- 問い合わせの初期ステータスは「要対応」とする。

## 2.2 システム管理者向け機能

### ツール管理

- 新規ツールは管理画面から登録せず、所定のSQLによってDBへ直接登録する。
- 新規ツールの初期状態は「非公開」とする。
- ツール管理画面の初期表示ではツール選択画面だけを表示し、選択後に対象ツールの更新画面を表示する。
- 基本情報、提供内容、バージョン、お知らせを個別の画面および保存処理で更新できる。
- 基本情報として、分類、ツール名、概要、利用者向け備考、担当者、表示順および状態を更新できる。
- ツールの状態を「使用可能」「メンテナンス中」「利用停止」「非公開」から設定できる。
- WebページURL等のツール提供内容を管理できる。
- デスクトップアプリケーションをアップロード・更新できる。
- Webツールとデスクトップツールのマニュアルをアップロード・更新できる。
- ツール固有のお知らせを登録・更新できる。
- ツールの分類および一覧の表示順を管理できる。
- デスクトップツールでは、アプリケーションのアップロードとバージョン更新情報を同じ画面から登録できる。
- Webツールでは、アプリケーションのアップロード欄を表示せず、バージョン更新情報だけを登録できる。
- バージョン更新情報を登録し、登録した履歴を自動的に最新バージョンとする。
- バージョン更新画面にはお知らせ登録欄を設けない。
- お知らせ更新画面で、登録済みのお知らせを確認できる。
- お知らせは非公開で保存、公開のみ、公開してメール送信のいずれかを選択できる。

### システムお知らせ管理

- システム全体のお知らせを登録・更新できる。
- 新規ツールの公開案内にはシステムのお知らせを使用する。
- お知らせの登録・更新欄と、登録済みお知らせの一覧を同じ画面に表示する。
- 登録済みお知らせは横長の固定高領域へ表示し、縦スクロールで確認できる。
- お知らせごとに公開・非公開を設定できる。
- お知らせは非公開で保存、公開のみ、公開してメール送信のいずれかを選択できる。
- メールを送信済みか、および送信日時を確認できる。

### ユーザー管理

- ユーザーを一覧表示し、ユーザー名・メールアドレス、権限および有効状態で検索・絞り込みできる。
- サービス開始後の追加ユーザーを管理画面から登録できる。
- メールアドレス、ユーザー名、権限および有効・無効を管理できる。
- ユーザーの最終利用日時を確認できる。
- ユーザーは物理削除せず、利用を停止する場合は無効化する。
- 新規ユーザー登録時にユーザー設定を初期値で同時作成する。
- ユーザーIDは画面に表示せず、登録時にSQL Server側でUUIDを自動生成する。
- ログイン中の管理者は、自分自身を無効化または一般ユーザーへ変更できない。
- 最後の有効なシステム管理者を無効化または一般ユーザーへ変更できない。
- ツール担当者および問い合わせ担当者には、有効なシステム管理者だけを選択できる。
- ポータル独自認証を採用した場合は、パスワードリセット、ログイン失敗によるロック状態の確認およびロック解除を追加する。
- Windows認証またはEntra IDを採用した場合は、外部認証基盤のアカウントロックやパスワードをポータルから管理しない。

### 問い合わせ管理

- 問い合わせを一覧表示し、問い合わせID、送信日時、カテゴリ、対象、送信者、担当者およびステータスを確認できる。
- ステータス、対象、カテゴリ、担当者および送信期間で一覧を絞り込める。
- 問い合わせの内容を確認できる。
- 担当者、ステータスおよび管理者用備考を更新できる。
- 添付ファイルは問い合わせ管理画面では管理しない。

### ログ・利用状況

- 期間、ツール、ユーザー、ログ種別、イベント種別、処理結果でログを絞り込める。
- ツール別の利用回数と利用ユーザー数を確認できる。
- Webツールを開いた回数を確認できる。
- デスクトップツールのダウンロード要求回数を確認できる。
- ツールの最終利用日時を確認できる。
- 利用状況を月別または指定期間全体で集計できる。
- 集計条件として期間、集計単位、大分類およびツールを指定できる。
- 集計結果をExcelで利用できるTSV形式でダウンロードできる。
- 初期段階ではシステム内にグラフを実装せず、TSVをExcelで開いて可視化する。
- ユーザーアクセスログとシステムエラーログを確認できる。

## 2.3 システム自動機能

### 利用履歴

- デスクトップツールのダウンロード要求を記録する。
- Webツールを開いた履歴を、各Webツールから共通DLLのメソッドを呼び出して記録する。
- Webツールで計算を実行した履歴を、各Webツールから共通DLLのメソッドを呼び出して記録する。
- 初期段階のツール利用履歴は、デスクトップツールのダウンロード要求、Webツールの起動およびWebツールの計算実行に絞る。

### ユーザーアクセスログ

- ポータルへのログインまたは利用開始を記録する。
- 認証方式に応じて、取得可能なログアウトおよびアクセス拒否を記録する。
- Users.LastAccessAtは、認証済みセッションの開始時だけ更新し、画面遷移やAPI呼び出しのたびには更新しない。

### システムエラーログ

- ポータル、共通API、各Webツールで発生したシステムエラーを記録する。

### お知らせメール通知

- 「公開してメール送信」が選択された場合は、編集内容を保存して公開した後にメールを送信する。
- システムのお知らせは、システムお知らせメールを受信する設定の有効なユーザーへ送信する。
- ツールのお知らせは、対象ツールをお気に入り登録し、お気に入りツールお知らせメールを受信する設定の有効なユーザーへ送信する。
- メール送信前に送信対象人数を表示し、確認を求める。
- メール送信中は送信ボタンを無効にし、二重送信を防止する。
- メール送信に成功した場合だけ、お知らせのメール送信日時を更新する。
- メール送信に失敗した場合も、お知らせの保存および公開は取り消さず、失敗内容をシステムエラーログへ記録する。
- お知らせを編集しただけではメールを再送しない。
- 送信済みのお知らせを再送する場合は、再送であることを表示して確認を求める。
- メール送信は送信ボタン押下時に即時実行し、定期バッチや送信待ちキューは初期実装に含めない。

### 問い合わせメール通知

- 問い合わせをDBへ登録し、採番された問い合わせIDを取得してからメールを送信する。
- メール件名は`[システム名]問い合わせ【[カテゴリ]】【No.[問い合わせID]】`とする。
- メール本文には枕文、問い合わせID、カテゴリ、対象および内容を記載する。
- メールのTOには問い合わせ送信者、BCCには対象に応じた担当者を設定する。
- 対象がシステムまたはその他の場合は、有効なシステム管理者をBCCへ設定する。
- 対象がツールの場合は、ツールマスタの担当者をBCCへ設定する。
- ツール担当者が未設定、無効またはメールアドレス未登録の場合は、有効なシステム管理者をBCCへ設定する。
- 添付ファイルがある場合はメールへ添付し、送信成功後に一時ファイルを削除する。
- メール送信に失敗しても問い合わせレコードは保持し、失敗内容をシステムエラーログへ記録する。
- 添付付きメールの送信失敗時における再送方法と一時ファイルの削除時期は、今後決定する。

---

# 3. 非機能要件

本章では、システム全体に求める品質、制約および運用上の前提を定義する。

## 3.1 セキュリティ

- HTTPSを使用する。
- 未認証ユーザーからのアクセスを制限する。
- 管理機能はシステム管理者のみ利用可能とする。
- DB接続には営業支援システム共通のアプリケーション用DBユーザーを使用する。
- DBユーザーには必要最小限の権限のみ付与する。
- 接続文字列やパスワード等をソースコードへ直接記述しない。
- SQLインジェクション、XSS、CSRFへの対策を実施する。
- 管理者がアップロードするファイルは、許可する拡張子と最大容量を制限する。
- 問い合わせの添付ファイルは、許可する拡張子と最大容量をサーバー側で検証する。

## 3.2 対応クライアント

- 会社支給のWindows端末を対象とする。
- 会社標準のMicrosoft Edgeを対応ブラウザとする。

## 3.3 可用性・性能

- 登録ユーザー約200名、同時利用約5名を前提とする。
- 大規模な分散システムやオートスケーリングは前提としない。
- 保守性と運用のしやすさを優先する。
- 画面ごとの厳密なレスポンスタイムは定義しない。
- 通常操作に支障がないことを検証環境で確認する。
- 長時間計算、大量データ処理、大容量帳票生成はツール単位で要件を定める。

## 3.4 共通エラー処理

- 本番環境では、例外の詳細およびスタックトレースを画面へ表示しない。
- 利用者には共通エラー画面と問い合わせ用の識別番号を表示する。
- 問い合わせ用の識別番号をシステムエラーログへ記録する。
- ポータルおよび各Webツールで共通のエラー表示形式を使用する。

## 3.5 障害分離

- 一つのWebツールの障害によって、ポータルや他のWebツールを停止させない。
- ツール停止中も、ポータルのツール一覧、詳細、お知らせを利用できる構成とする。
- Webツールごとに独立して停止、更新、再起動できる構成とする。
- メール送信の失敗によって、お知らせの登録・更新を失敗させない。
- 共通API障害時の影響範囲を設計工程で明確にする。

## 3.6 保守性・拡張性

- ポータルと各ツールを個別のソリューションに分割する。
- ツール単位で改修、テスト、リリースできる構成とする。
- 共通処理は共通APIまたは共通ライブラリとして提供する。
- 各ツールの実装方式を可能な限り統一する。
- ツール開発用の標準テンプレートとサンプルツールを用意する。
- 新規ツールは、所定のSQLで非公開状態として登録し、その後に管理画面から基本情報、提供内容、バージョンおよび状態を設定できるようにする。

## 3.7 データ・ログ保持

- 計算結果および帳票データは原則としてシステム側へ保持しない。
- 問い合わせの添付ファイルはメール送信時だけ一時保存し、恒久保存しない。
- 入力内容、計算結果、帳票内容、パスワード、接続文字列、認証情報をログへ記録しない。
- ツール利用履歴は原則として削除せず保持する。
- ユーザーアクセスログは6か月間保持する。
- システムエラーログは6か月間保持する。
- 保持期間を超えたユーザーアクセスログおよびシステムエラーログは定期削除する。

## 3.8 環境識別・画面テーマ

- 開発環境と本番環境は設定ファイルの値によって判定する。
- 環境の判定をビルド構成、URLまたは利用者の選択だけに依存させない。
- 画面上部の帯は、本番環境では青、開発環境では赤を既定色とする。
- 色の違いだけでなく、帯に「本番環境」または「開発環境」の文字を表示する。
- 初期開発では、本文領域に社内システム統一テーマを適用する。
- 本文テーマは設定ファイルで指定できる構成とし、開発環境でも開発用設定によってテーマ表示を確認できるようにする。
- 開発環境では本文テーマの設定にかかわらず、環境を示す赤い帯を固定表示する。
- 本番環境では本文テーマの設定にかかわらず、環境を示す青い帯を固定表示する。
- ポータルおよび各Webツールで同じ環境判定と帯の表示ルールを使用する。
- 社内システム統一テーマを標準とし、独自CSSでは固定色の直接指定を可能な限り避ける。
- 利用者ごとのテーマ選択は初期対象外とする。
- 将来のダークモード追加を考慮し、Bootstrapのテーマ機能およびCSS変数を使用できる構成とする。

---

# 4. 画面構成

## 4.1 画面一覧

| ID | 画面 | 利用者 | 主な目的 |
| --- | --- | --- | --- |
| P001 | ログイン | 全ユーザー | ポータルへ認証する。外部認証採用時は不要となる場合がある |
| P002 | ポータルトップ | 全ユーザー | お知らせと主要機能への入口を表示する |
| P003 | ツール一覧 | 全ユーザー | 非公開を除くすべてのツールを一覧表示する |
| P004 | ツール詳細 | 全ユーザー | お知らせ、マニュアル、ツール起動・取得を提供する |
| P005 | 利用マニュアル・FAQ | 全ユーザー | ポータルの利用方法を確認する |
| P006 | 個人設定 | 全ユーザー | 通知設定を変更する |
| P007 | 問い合わせ・ご意見 | 全ユーザー | 問い合わせ、ご意見、不具合等を送信する |
| P008 | お気に入りツール | 全ユーザー | お気に入り登録したツールを一覧表示する |
| A001 | ツール管理 | システム管理者 | ツール情報、状態、提供内容、バージョン、お知らせを管理する |
| A002 | ユーザー管理 | システム管理者 | ユーザー情報と権限を管理する |
| A003 | ログ・利用状況 | システム管理者 | ログの検索と利用状況の集計を行う |
| A004 | システムお知らせ管理 | システム管理者 | ポータル全体のお知らせを管理する |
| A005 | 問い合わせ管理 | システム管理者 | 問い合わせの検索と対応状況の更新を行う |

各Webツール内部の画面は、各ツール側で管理する。

## 4.2 ポータルトップレイアウト

```text
┌──────────────────────────────────────────────────────────────┐
│ 営業支援ポータル［本番環境／開発環境］        ユーザー情報   │
├──────────────────────────────────────────────────────────────┤
│ ■ お知らせ                                                  │
│  システムのお知らせを表示                                  │
├──────────────────┬──────────────────┬────────────────────────┤
│ ツール一覧       │ サポート         │ 管理画面               │
│                  │                  │ ※システム管理者のみ   │
│ ・全ツール       │ ・個人設定       │ ・ツール管理           │
│ ・お気に入り     │ ・利用マニュアル │ ・ユーザー管理         │
│                  │   ・FAQ（同画面）│ ・ログ・利用状況       │
│                  │ ・問い合わせ・   │ ・システムお知らせ管理 │
│                  │   ご意見         │ ・問い合わせ管理       │
└──────────────────┴──────────────────┴────────────────────────┘
```

- 「全ツール」からツール一覧画面へ移動する。
- 「お気に入り」からお気に入りツール画面へ移動する。
- サポートの各項目から対応する画面へ移動する。
- 管理画面の列はシステム管理者にのみ表示する。
- 最上部の帯に環境名を表示し、本番環境は青、開発環境は赤で表示する。

## 4.3 個人設定画面

```text
■ 通知設定

システムのお知らせ
[ メールを受信する ]

お気に入りツールのお知らせ
[ メールを受信する ]

                                      [ 保存 ]
```

- 通知設定では、システムお知らせメールとお気に入りツールお知らせメールの受信要否を設定する。
- 通知設定の既定値は、いずれも「受信する」とする。
- 初期開発では画面テーマの選択項目を設けない。
- 画面には社内システム統一テーマを使用し、ダークモード等の利用者別テーマは将来拡張とする。
- プロフィール画像、アイコン、表示名等を利用者が変更する機能は設けない。

## 4.4 ツール一覧・お気に入り画面

### 一覧の表示項目

```text
☆｜大分類｜ツール名｜ツール概要｜バージョン｜更新日｜状態｜＞
```

- 一つのツールを帯形式の一行で表示する。
- 全ツール画面とお気に入りツール画面で、表示項目、並び順および操作方法を統一する。
- 星印の選択ではお気に入りの登録・解除のみを行い、詳細画面へは移動しない。
- 行本体、ツール名または右端の詳細アイコンからツール詳細画面へ移動する。
- お気に入り画面でお気に入りを解除したツールは、画面から除外する。
- 初期表示順は、大分類のソート順、ツールのソート順、ツール名の順とする。
- バージョンと更新日は、最新フラグが設定されたツール履歴から取得する。
- 一覧上では提供方式を表示せず、利用者がWebツールかデスクトップツールかを意識しない構成とする。

### 状態ごとの表示・操作

| 状態 | 一般ユーザーの一覧 | 詳細画面 | ツールの起動・取得 | お知らせ・更新情報・マニュアル |
| --- | --- | --- | --- | --- |
| 使用可能 | 表示する | 表示できる | 利用できる | 参照できる |
| メンテナンス中 | 表示する | 表示できる | 利用不可 | 参照できる |
| 利用停止 | 表示する | 表示できる | 利用不可 | 参照できる |
| 非公開 | 表示しない | 表示不可 | 利用不可 | 表示しない |

- 非公開になったツールのお気に入り情報は削除せず、再公開時に復元できるようにする。
- 非公開ツールの詳細URLへ一般ユーザーが直接アクセスした場合は、権限エラーまたは対象なしとして扱う。
- 各Webツールは共通の入口処理でツール状態を確認し、ポータルを経由しない直接URLへのアクセスにも状態制御を適用する。
- 「メンテナンス中」および「利用停止」は共通の案内画面を表示し、「非公開」は権限エラーまたは対象なしとして扱う。
- IIS側での停止はアプリケーション障害や配備作業時の運用手段とし、通常の状態切替には使用しない。
- 既に利用者端末へ保存されたデスクトップアプリケーションの起動はポータルから制御できないため、状態による制御対象は新規ダウンロードとする。

## 4.5 ツール詳細画面

ツール詳細画面は、次の順で構成する。

1. ツールのお知らせ
   - 公開中の障害情報、メンテナンス予定、利用上の注意等を、対象ツールを参照できる全ユーザーへ表示する。
2. バージョン更新情報
   - 最新バージョンを先頭に、バージョン、更新内容、更新日、修正担当者を表示する。
3. ツール概要
   - 一覧の一行に相当する、お気に入り、大分類、ツール名、概要、最新バージョン、更新日、状態を表示する。
4. 利用者向け備考
   - ツールの利用上の注意事項や補足を表示する。
5. ツールの提供内容
   - 登録内容に応じて、Webページリンク、デスクトップアプリケーション、マニュアル、データリンク等を表示する。

## 4.6 利用マニュアル・FAQ画面

利用マニュアルとFAQは個別の画面に分けず、一つの画面内に次の順で表示する。

### 利用マニュアル

- ポータルサイトの操作方法を記載したPDFへのリンクを表示する。
- PDFはサービスサーバー上の所定フォルダーへ配置する。
- サーバー名やドメイン名を含む絶対URLではなく、`/manuals/sales-support-portal-manual.pdf`等のサイト内相対URLを画面へ記載する。
- PDFを差し替えた際のブラウザーキャッシュを考慮し、版を含むファイル名または更新番号を付けたURLを使用する。

### FAQ

```text
No.1　カテゴリ：ログイン
Q　ログインできない場合はどうすればよいですか？
A　社内ネットワークへの接続状態を確認してください。
```

- 一つのFAQを質問と回答の二段で表示する。
- NoはDBへ保持せず、画面の表示順に従って採番する。
- FAQカテゴリはカテゴリマスタのソート順で表示する。
- 各カテゴリ内のFAQはFAQテーブルのソート順で表示する。
- FAQの質問と回答はプレーンテキストと改行を基本とし、任意のHTML入力は許可しない。
- 非公開のFAQは一般ユーザーへ表示しない。
- FAQカテゴリおよびFAQは管理画面を設けず、所定のSQLによってDBへ直接登録・更新する。

## 4.7 問い合わせ・ご意見画面

```text
カテゴリ       [ 質問／要望／意見／不具合報告／その他 ▼ ]
対象           [ システム／各ツール／その他 ▼ ]
内容           [                                      ]
               [                                      ]
添付ファイル   [ ファイルを選択 ]  ※任意、1ファイル

                                      [ 送信 ]
```

- カテゴリと対象はドロップダウンリストから選択する。
- 内容は必須入力とし、最大2,000文字とする。
- 非公開ツールは対象の選択肢に表示しない。
- メンテナンス中または利用停止のツールは対象の選択肢に表示する。
- 要望および意見は原則として個別回答を行わず、質問、不具合報告およびその他は回答対象とする旨をカテゴリ選択時に表示する。
- 添付ファイルはPDF、Excel、Word、CSV、テキスト、PNG、JPEGのいずれか一ファイル、最大10MBとする。
- 送信完了時に問い合わせIDを表示する。

## 4.8 問い合わせ管理画面

### 一覧・絞り込み

- 問い合わせID、送信日時、カテゴリ、対象、送信者、担当者およびステータスを一覧表示する。
- ステータス、対象、カテゴリ、担当者および送信期間で絞り込める。
- 初期表示では「要対応」「対応中」「検討中」を優先して表示する。
- 一覧から問い合わせ詳細を表示できる。

### 詳細・更新

- 問い合わせID、送信日時、カテゴリ、対象、送信者および内容を表示する。
- 担当者、ステータスおよび管理者用備考を更新できる。
- ステータスは「要対応」「対応中」「検討中」「完了」「対応不要」から選択する。
- ツールが対象の場合はツール担当者を初期担当者とし、システムまたはその他の場合は担当者未割当を初期値とする。
- 添付ファイルは保存しないため、管理画面では表示およびダウンロードを行わない。
- 必要な添付ファイルは、問い合わせIDを使用して通知メールから確認する。

## 4.9 ツール利用フロー

```text
全ツール／お気に入り
   ↓
ツール詳細
   ├─ お知らせ・バージョン更新情報を確認
   ├─ Webページを開く
   ├─ デスクトップアプリケーションをダウンロード
   ├─ マニュアルをダウンロード
   └─ データリンクを開く
```

## 4.10 ツール管理画面

### ツール選択

- 初期表示ではツール選択画面だけを表示する。
- 分類、ツール名、担当者、状態および最新バージョンを一覧表示する。
- 分類および状態で一覧を絞り込める。
- 非公開のツールを含むすべてのツールを選択対象とする。
- ツール選択後に、基本情報、提供内容、バージョン、お知らせの各更新画面を表示する。
- 新規ツール登録画面は設けない。

```text
○○ツール　管理

[1. 基本情報] [2. 提供内容] [3. バージョン] [4. お知らせ]
```

- 各画面は独立した保存ボタンと更新処理を持ち、ほかの画面の項目を更新しない。
- ツールIDをURLに含め、再読み込み後も選択中のツールと画面を特定できるようにする。
- 共通監査列のUpdateCountを使用して同時更新を検出する。

### 1. 基本情報

- 分類、ツール名、概要、利用者向け備考、担当者、表示順および状態を編集できる。
- ツール種別は「デスクトップ」または「Web」を参照表示し、通常の更新項目には含めない。
- 状態をメンテナンス中、利用停止または非公開へ変更する場合は確認を求める。
- デスクトップツールをWeb化する場合は、当面は新しいWebツールとして別のToolIdで登録する。
- 旧デスクトップツールの利用停止時期、お気に入りや履歴の引継ぎ等の移行運用は今後決定する。

### 2. 提供内容

- WebページURL、マニュアルおよびデータリンクを個別に更新できる。
- マニュアルはツールのバージョン更新とは独立して更新できる。
- ファイル更新に失敗した場合は、以前の配布ファイルおよびマニュアルを維持する。

### 3. バージョン

- 現在のバージョンと過去のバージョン履歴を表示する。
- バージョン、修正担当者、更新内容およびバージョン更新日を登録できる。
- デスクトップツールでは、新しいアプリケーションファイルとバージョン更新情報を同じ操作で登録する。
- ツール種別がWebの場合はアプリケーションのアップロード項目を常に非表示とし、バージョン更新情報だけを登録する。
- 新しいバージョンを登録した場合は、以前の最新履歴を解除し、新しい履歴を最新として自動設定する。
- バージョン更新画面にはお知らせ登録欄を表示しない。
- バージョン更新を理由とするお知らせ登録やメール送信は自動実行しない。

### 4. お知らせ

- お知らせのタイトル、内容および公開フラグを編集できる。
- お知らせの登録・更新欄と、登録済みお知らせの一覧を同じ画面に表示する。
- 登録済みお知らせは横長の固定高領域へ埋め込み、縦スクロールで確認できるようにする。
- 登録済みお知らせは更新日の新しい順で表示する。
- 一覧の行を選択すると、その内容を編集欄へ読み込む。
- 公開・非公開とメール送信済み・未送信を見分けられるようにする。
- 操作ボタンは「非公開で保存」「公開のみ」「公開してメール送信」とする。
- 「公開してメール送信」は、編集内容を保存して公開した後、お気に入り登録済みかつ通知有効なユーザーへメールを送信する。
- 編集または公開だけではメールを自動送信しない。

## 4.11 システムお知らせ管理画面

ツールのお知らせ更新画面と同じ共通画面部品、保存処理および操作方式を使用する。

- システムお知らせのタイトル、内容および公開フラグを登録・編集できる。
- 新規ツールの公開案内はシステムお知らせとして登録する。
- お知らせの登録・更新欄と、登録済みお知らせの一覧を同じ画面に表示する。
- 登録済みお知らせは横長の固定高領域へ埋め込み、縦スクロールで確認できるようにする。
- 登録済みお知らせは更新日の新しい順で表示する。
- 一覧の行を選択すると、その内容を編集欄へ読み込む。
- 公開・非公開とメール送信済み・未送信を見分けられるようにする。
- 操作ボタンは「非公開で保存」「公開のみ」「公開してメール送信」とする。
- 「公開してメール送信」は、編集内容を保存して公開した後、システムお知らせメールの通知が有効なユーザーへメールを送信する。
- 公開中のお知らせだけを一般ユーザーのポータルトップへ表示する。
- 非公開にしたお知らせは削除せず、管理画面で確認できる。
- メール送信済みのお知らせには送信日時を表示する。
- 編集または公開だけではメールを自動送信しない。

## 4.12 利用状況集計画面

### 集計条件

- 期間、集計単位、大分類およびツールを指定できる。
- 集計単位は「月別」または「期間全体」から選択する。
- 期間には過去7日、過去30日、過去90日、当月、前月および任意期間等の指定方法を検討する。

### 集計結果

- ツールごとの利用ユーザー数、Webツール起動回数、デスクトップツールのダウンロード要求回数、Webツール計算実行回数および最終利用日時を表示する。
- 利用実績がないツールも0件として表示できるようにする。
- 初期段階では画面内にグラフを実装せず、数値表による確認を基本とする。
- 集計結果をTSV形式でダウンロードできる。

### TSV出力

- 月別出力では、集計月、大分類、ツールID、ツール名、利用ユーザー数、各イベント回数および最終利用日時を出力する。
- 期間全体出力では、集計開始日、集計終了日、大分類、ツールID、ツール名、利用ユーザー数、各イベント回数および最終利用日時を出力する。
- Excelで扱いやすい縦持ち形式、タブ区切り、UTF-8 BOM付きで出力する。
- ファイル名には集計単位と対象期間を含める。
- 文字列がExcelの数式として解釈されないように対策する。
- 出力したTSVをExcelで開き、ツール別または月別の棒グラフ等を作成する運用を想定する。

### 集計上の留意事項

- 月別の利用ユーザー数は同一ユーザーを各月で一人として数え、期間全体では期間内の重複を除いて数えるため、月別人数の合計と期間全体人数は一致しない場合がある。
- デスクトップツールは実行履歴を取得しないため、利用状況はダウンロード要求を基準とし、既に取得済みのアプリケーションの実行は集計できない。
- 利用ユーザーの定義、対象とするイベント、画面表示項目およびTSV項目の詳細は、利用者との協議によって決定する。

## 4.13 ユーザー管理画面

### 一覧・検索

```text
ユーザー名・メールアドレス [                    ]
権限                         [すべて ▼]
状態                         [すべて ▼]

[検索]                                      [新規登録]

ユーザー名｜メールアドレス｜権限｜状態｜最終利用日時｜編集
```

- ユーザー名・メールアドレス、権限および有効状態で検索・絞り込みできる。
- ユーザー名、メールアドレス、権限、状態および最終利用日時を一覧表示する。
- 最終利用日時はUsers.LastAccessAtから取得する。
- LastAccessAtは認証済みセッション開始時に更新し、個々の画面アクセスでは更新しない。
- 無効なユーザーも一覧へ表示できる。

### 新規登録・編集

- ユーザー名、メールアドレス、権限および有効状態を登録・編集できる。
- ユーザーIDはSQL Server側で自動生成し、画面には表示しない。
- メールアドレスの重複を許可しない。
- 新規登録時にUserPreferencesを初期値で同時作成する。
- ユーザーの物理削除機能は設けない。
- ユーザー情報の更新時は共通監査列のUpdateCountで同時更新を検出する。
- ログイン中の管理者自身と最後の有効な管理者に対する無効化・降格を禁止する。

### 認証方式による追加機能

- ポータル独自認証の場合は、パスワードリセット、ロック状態の確認およびロック解除を表示する。
- Windows認証またはEntra IDの場合は、外部認証基盤のパスワード変更およびアカウントロック解除機能を表示しない。

---

# 5. システム構成・責任分界

## 5.1 全体構成

```text
営業支援ポータル
│
├─ Portal
│   ├─ 認証・ユーザー管理
│   ├─ ツール・お知らせ管理
│   ├─ お気に入り・通知
│   ├─ ログ・利用状況
│   ├─ 問い合わせ・問い合わせ管理
│   └─ 共通API・共通UI
│
├─ Tool001
├─ Tool002
├─ Tool003
│   ...
└─ Tool020
```

## 5.2 ポータル側の責務

- 認証・認可
- ユーザー管理
- ツール情報・状態管理
- デスクトップアプリケーション配布
- マニュアル配布
- お気に入り
- システム・ツールのお知らせ
- メール通知
- 問い合わせ受付・問い合わせ管理
- デスクトップツールのダウンロード要求履歴の記録
- 利用履歴・ログの参照および集計
- 共通UI・共通API・共通ライブラリ
- ツールへの遷移

## 5.3 各ツール側の責務

- ツール固有画面
- 業務ロジック・計算処理
- 業務データの登録、更新、検索
- 帳票作成・ダウンロード
- ファイル生成
- ツール固有の入力チェックとエラー処理
- 共通DLLのメソッドを使用したWebツール起動・計算実行履歴の登録
- 共通の入口処理によるツール状態の確認と直接URLアクセスの制御
- 使用した一時ファイルの削除

## 5.4 URL構成案

```text
https://sales-support/
├─ /                       ポータルトップ
├─ /tools                  ツール一覧
├─ /tools/favorites        お気に入りツール
├─ /tools/T001             Tool001詳細
├─ /tools/T001/app         Tool001 Webアプリ
├─ /support                サポート
├─ /support/inquiries      問い合わせ・ご意見
├─ /settings               個人設定
├─ /admin                  管理機能
├─ /admin/tools            ツール選択
├─ /admin/tools/T001/basic ツール基本情報
├─ /admin/tools/T001/resources 提供内容
├─ /admin/tools/T001/versions  バージョン
├─ /admin/tools/T001/notices   ツールのお知らせ
└─ /admin/inquiries        問い合わせ管理
```

内部的に別Webアプリケーションであっても、利用者からは同一ドメイン配下の一つのサイトとして見える構成を目指す。

---

# 6. 技術・開発環境

## 6.1 基本技術

| 領域 | 技術 |
| --- | --- |
| 開発エディター | Visual Studio Code |
| プラットフォーム | .NET 10 LTS |
| Webフレームワーク | ASP.NET Core 10 MVC |
| 開発言語 | C# |
| View | Razor |
| UI | Bootstrap |
| クライアント処理 | JavaScript |
| DB | SQL Server |
| Webサーバー | IIS |

## 6.2 開発上の留意事項

- 新規開発では.NET Frameworkではなく.NET 10 LTSを使用する。
- 社内で使用中のASP.NET、VB.NET、.NET Framework 4.7とは構成や開発方式が異なることに留意する。
- 開発開始前に標準テンプレート、サンプルツール、コーディング規約を用意する。
- Visual Studio Codeで使用するC#拡張機能の機能とライセンスを確認する。
- .NET SDKのバージョンを統一する。

## 6.3 Webアプリケーション構成

```text
Controller
    ↓
Service
    ↓
データアクセス
    ↓
SQL Server
```

- ControllerはHTTPリクエスト、画面遷移、入力受付を担当する。
- Serviceは計算処理等の業務ロジックを担当する。
- データアクセスにはEF CoreのDbContextをServiceから利用する構成、またはRepositoryを介する構成を選択できる。
- Repository層を全ツールへ必須とするかは、採用するデータアクセス技術と標準化の費用対効果を踏まえて今後決定する。
- Dependency Injectionを利用して各層を疎結合にする。

## 6.4 ソリューション構成

```text
SalesSupport.Portal.sln
├─ SalesSupport.Portal
├─ SalesSupport.Api
└─ Portal関連プロジェクト

SalesSupport.Tool001.sln
├─ SalesSupport.Tool001.Web
├─ SalesSupport.Tool001.Application
└─ SalesSupport.Tool001.Tests

SalesSupport.Tool002.sln
├─ SalesSupport.Tool002.Web
├─ SalesSupport.Tool002.Application
└─ SalesSupport.Tool002.Tests
```

- ポータルと各ツールは個別のソリューションとして管理する。
- 複数開発者が互いのツールへの影響を抑えて並行開発できる構成とする。
- Common.dllはローカルNuGetパッケージ等でバージョン管理し、各ソリューションから明示したバージョンを参照する。
- Common.dllだけを稼働中の各ツールの`bin`へ上書きせず、更新時は各ツールを復元、ビルド、テスト、発行した成果物単位で配備する。

## 6.5 環境・テーマ設定

- ポータルおよび各Webツールは、設定ファイルから環境区分を取得する。
- 環境区分は`Production`または`Development`とし、共通設定クラスから参照する。
- 環境区分と表示の対応は、本番環境を青帯、開発環境を赤帯とする。
- 環境区分は利用者が画面から変更できないようにする。
- 本文テーマは設定ファイルから取得し、初期値を社内システム統一テーマとする。
- 開発環境でも本文テーマの表示確認は可能とするが、環境を示す赤い帯は固定する。
- 環境判定、テーマ適用および帯表示は共通UIへ実装し、ポータルと各Webツールで再利用する。
- 初期開発では社内システム統一テーマのみを実装し、利用者ごとのテーマ選択とダークモードは拡張想定とする。

---

# 7. DB・共通基盤

## 7.1 DB基本方針

- SQL Server上に営業支援システム専用DBを用意する。
- ポータルと各ツールは、営業支援システム共通のアプリケーション用DBユーザーで接続する。
- 営業支援システムに不要な他システムのDBへ直接アクセスしない。
- DBユーザーには必要最小限の権限のみ付与する。
- 接続文字列や認証情報は各アプリケーションの設定からDependency Injection等で共通DLLへ渡し、Common.dll内へ埋め込まない。
- DBスキーマはdboを使用する案を基本候補とし、ツール単位で分割するかは今後決定する。

## 7.2 共通テーブルカラム

すべてのシステムテーブルに次の共通監査列を設ける。個別テーブルの項目一覧には、これらの共通監査列を記載しない。

| 項目 | 内容 |
| --- | --- |
| UpdateCount | 更新回数。登録時は0とし、更新ごとに加算する |
| CreatedAt | 作成日時 |
| CreatedBy | 作成時のDBユーザー |
| UpdatedAt | 更新日時 |
| UpdatedBy | 更新時のDBユーザー |

- 共通監査列はDEFAULT制約またはDBトリガー等によりDB側で自動設定する。
- 共通監査列とトリガーは共通のDDLテンプレートから作成し、テーブルごとに個別仕様を持たせない。
- トリガーの共通動作はテンプレートに対する共通テストで確認する。
- アプリケーションの内部ユーザーIDをDBセッションへ受け渡して監査列へ設定する仕組みは設けない。
- アプリケーション上の送信者、担当者、修正担当者等、業務上必要なユーザー情報は各テーブルの業務項目として保持する。
- 日時の基準、DBユーザー名の取得方法およびトリガーの共通化方法は詳細設計で決定する。

## 7.3 ユーザーデータ

- ユーザーごとにシステム内部で一意の内部ユーザーIDを付与する。
- 内部ユーザーIDは画面には表示しない。
- 内部ユーザーIDは、メールアドレスやユーザー名が変更されても変更しない。
- ログイン時の識別子と内部ユーザーIDの紐付け方法は、採用する認証方式に応じて決定する。
- 外部認証を採用する場合は、外部認証基盤の識別子を内部ユーザーIDへ紐付ける。
- パスワード関連の項目およびテーブルは、認証方式が決定するまで本構成には含めない。

### ユーザーテーブル（Users）

| 項目 | 内容 |
| --- | --- |
| UserId | `uniqueidentifier`型の内部ユーザーID。SQL Server側でUUIDを自動生成する |
| Email | メールアドレス |
| UserName | 画面表示用のユーザー名 |
| RoleCode | USERまたはADMIN |
| IsActive | ユーザーの有効・無効 |
| LastAccessAt | 最終利用日時。未利用の場合はNULL |

- UserIdには`DEFAULT NEWSEQUENTIALID()`を設定し、アプリケーションおよび直接投入SQLから値を指定しない。
- メールアドレスには一意制約を設定する。
- RoleCodeはUSERまたはADMINだけを許可する。
- IsActiveの初期値は有効とする。
- 無効なユーザーはポータルを利用できない。
- 初期段階では一般ユーザーとシステム管理者の二種類とし、権限マスタは設けない。
- LastAccessAtは認証済みセッションの開始時だけ更新し、リクエストのたびには更新しない。

### ユーザー設定テーブル（UserPreferences）

| 項目 | 内容 |
| --- | --- |
| UserId | 内部ユーザーID。主キー兼Usersへの外部キー |
| SystemNoticeMailEnabled | システムお知らせメールの受信要否 |
| FavoriteToolNoticeMailEnabled | お気に入りツールお知らせメールの受信要否 |

- Usersと一対一で保持する。
- 通知設定の初期値はいずれも有効とする。

### お気に入りテーブル（UserToolFavorites）

| 項目 | 内容 |
| --- | --- |
| UserId | 内部ユーザーID |
| ToolId | ツールID |

- UserIdとToolIdの組み合わせを複合主キーとする。
- 同一ユーザーが同一ツールを重複登録できないようにする。
- UsersおよびToolsへの外部キーを設定する。
- ツールが非公開となった場合もレコードを削除しない。

### 初期ユーザー一括登録

- サービス開始時のユーザーは、所定のSQLスクリプトでDBへ直接一括登録する。
- 一括登録ではUserIdを指定せず、UsersのDEFAULT制約によってUUIDを生成する。
- `OUTPUT inserted.UserId`等を使用して生成されたUserIdを取得し、同一トランザクション内でUserPreferencesを作成する。
- UserPreferencesの初期値は、システム通知有効、お気に入りツール通知有効とする。
- 一括登録処理はトランザクション内で実行し、UsersまたはUserPreferencesの登録に失敗した場合は全体をロールバックする。
- 登録前にメールアドレスの重複、RoleCode、登録件数および有効な管理者の存在を検証する。
- 本番環境で任意のINSERT文を都度作成せず、検証済みの初期ユーザー登録用SQLとして管理する。

## 7.4 ツール管理データ

### ツール分類マスタ（ToolCategories）

| 項目 | 内容 |
| --- | --- |
| CategoryId | 分類を識別するID |
| CategoryName | 一覧に表示する大分類名 |
| SortOrder | 分類の表示順 |

### ツールマスタ（Tools）

| 項目 | 内容 |
| --- | --- |
| ToolId | ツールを識別するID |
| CategoryId | 所属する分類ID |
| ToolName | ツール名 |
| ToolSummary | ツール概要 |
| Remarks | ツール詳細に表示する利用者向け備考 |
| OwnerUserId | 担当者となるシステム管理者の内部ユーザーID |
| ToolType | DESKTOPまたはWEB |
| Status | 使用可能、メンテナンス中、利用停止、非公開 |
| SortOrder | 分類内の表示順 |

- バージョンおよび更新日はツールマスタへ重複して保持せず、最新フラグが設定されたツール履歴から取得する。
- ToolTypeは管理画面の表示制御に使用し、一般ユーザー向けツール一覧には表示しない。
- 新規ツールは所定のSQLスクリプトまたはストアドプロシージャで直接登録し、Statusの初期値を非公開とする。
- OwnerUserIdには、有効かつRoleCodeがADMINのユーザーだけを設定できるようにする。
- デスクトップツールをWeb化する場合は、当面は新しいWebツールとして別のToolIdで登録する。
- 旧デスクトップツールの停止方法や各種データの引継ぎは、移行運用の決定後に定める。

### ツール履歴（ToolVersionHistories）

| 項目 | 内容 |
| --- | --- |
| ToolHistoryId | ツール履歴を識別するID |
| ToolId | 対象ツールID |
| Version | バージョン |
| ModifiedByUserId | 修正担当者の内部ユーザーID |
| ChangeDescription | 更新内容 |
| ReleasedAt | バージョン更新日 |
| IsCurrent | 一覧・概要へ表示する最新履歴かを示すフラグ |

- 一つのツールに対して、最新フラグを設定できる履歴は一件のみとする。
- 最新フラグの更新はトランザクション内で行い、最新履歴が複数発生しないようDB制約も設ける。
- バージョン文字列や更新日の大小だけでは最新を判定しない。
- 履歴は更新せず追加を基本とし、過去のバージョン更新情報として詳細画面へ表示する。
- ToolIdとVersionの組み合わせに一意制約を設定する。
- バージョン履歴の登録では、お知らせレコードを作成しない。

### ツール提供内容（ToolResources）

| 項目 | 内容 |
| --- | --- |
| ToolResourceId | 提供内容を識別するID |
| ToolId | 対象ツールID |
| ResourceType | Webページ、デスクトップアプリ、マニュアル、データリンク等の区分 |
| DisplayName | 詳細画面に表示する名称 |
| ResourceLocation | URLまたは管理ファイルを参照する情報 |
| SortOrder | 詳細画面内の表示順 |
| IsEnabled | 提供内容の有効・無効 |

- ツールの提供方式は一覧の表示項目には含めず、登録された提供内容に応じて詳細画面を構成する。
- ToolTypeがWEBの場合は、デスクトップアプリケーションの提供内容を登録・有効化できないようにする。
- ファイルの実体管理に必要なファイル名、保存先等の詳細項目は詳細設計で決定する。

## 7.5 お知らせデータ

### お知らせテーブル（Notices）

| 項目 | 内容 |
| --- | --- |
| NoticeId | お知らせID |
| NoticeType | SYSTEMまたはTOOL |
| ToolId | ツールお知らせの場合の対象ツールID。システムお知らせの場合はNULL |
| Title | タイトル |
| Content | お知らせ内容 |
| IsPublished | 公開フラグ。初期値は非公開 |
| MailSentAt | メール送信日時。未送信の場合はNULL |

- システムお知らせとツールお知らせを一つのNoticesテーブルで管理する。
- NoticeTypeがSYSTEMの場合はToolIdをNULL、TOOLの場合はToolIdを必須とするDB制約を設ける。
- 一般ユーザーにはIsPublishedが有効なお知らせだけを表示する。
- システムお知らせはポータルトップ、ツールお知らせは対象ツールの詳細画面へ表示する。
- ツール詳細画面のお知らせは、対象ツールを参照できる全ユーザーへ表示する。
- お知らせを非公開にしてもレコードを削除しない。
- MailSentAtはメール送信に成功した場合だけ設定する。
- お知らせの保存、編集または公開だけではメールを自動送信しない。
- システムお知らせの送信対象は、SystemNoticeMailEnabledが有効なユーザーとする。
- ツールお知らせの送信対象は、対象ツールのお気に入り登録者のうちFavoriteToolNoticeMailEnabledが有効なユーザーとする。
- メールは「公開してメール送信」の操作時に即時送信する。
- 送信済みのお知らせを再送した場合は、MailSentAtを最新の送信日時へ更新する。

## 7.6 FAQデータ

### FAQカテゴリマスタ（FaqCategories）

| 項目 | 内容 |
| --- | --- |
| CategoryId | FAQカテゴリを識別するID |
| CategoryName | FAQ画面に表示するカテゴリ名 |
| SortOrder | カテゴリの表示順 |

### FAQテーブル（FaqItems）

| 項目 | 内容 |
| --- | --- |
| FaqId | FAQを識別するID |
| CategoryId | 所属するFAQカテゴリID |
| Question | 質問内容 |
| Answer | 回答内容 |
| SortOrder | カテゴリ内の表示順 |
| IsPublished | FAQを一般ユーザーへ表示するかを示すフラグ |

- 画面上は質問と回答を二段で表示するが、DB上は一つのFAQを一レコードとして保持する。
- 質問と回答を`QorA`で別レコードに分割しない。
- 同一カテゴリ内では、FAQのソート順が重複しないようにする。
- FaqCategoriesおよびFaqItemsは管理画面から更新せず、検証済みの所定SQLで直接登録・更新する。
- 利用マニュアルPDFのURLはDBで管理せず、利用マニュアル・FAQ画面にサイト内相対URLとして記載する。

## 7.7 問い合わせデータ

### 問い合わせテーブル（Inquiries）

| 項目 | 内容 |
| --- | --- |
| InquiryId | 主キー兼、画面とメールに表示する問い合わせID |
| CategoryCode | 質問、要望、意見、不具合報告、その他 |
| TargetType | システム、ツール、その他 |
| ToolId | 対象がツールの場合のツールID。その他の場合はNULL |
| SubmittedByUserId | 問い合わせを送信した内部ユーザーID |
| Content | 問い合わせ内容 |
| Status | 要対応、対応中、検討中、完了、対応不要。初期値は要対応 |
| AssigneeUserId | 担当者の内部ユーザーID。未割当を許可する |
| AdminNote | 管理者用備考 |

- 表示専用の問い合わせ番号や別の内部IDは設けず、InquiryIdを主キー兼問い合わせ番号として使用する。
- 対象がツールの場合は、ツールマスタの担当者を初期担当者とする。
- 対象がシステムまたはその他の場合は、初期担当者を未割当とする。
- AssigneeUserIdには、有効かつRoleCodeがADMINのユーザーだけを設定できるようにする。
- 添付ファイルの内容、ファイル名および保存先はDBへ保持しない。
- 添付ファイル用テーブルは設けない。

## 7.8 ログテーブル

| テーブル | 用途 | 保持方針 |
| --- | --- | --- |
| ToolUsageLogs | デスクトップツールのダウンロード要求、Webツール起動、Webツール計算実行 | 原則として削除しない |
| UserAccessLogs | ログイン、ポータル利用開始、ログアウト、アクセス拒否等 | 6か月間保持する |
| SystemErrorLogs | ポータル、共通API、各Webツールのシステムエラー | 6か月間保持する |

- 内部ユーザーID、ツールID、発生日時、イベント種別、処理結果等を記録する。
- 必要に応じて相関IDを付与する。
- 保持期間を超えたユーザーアクセスログおよびシステムエラーログは定期削除する。
- システムエラーログはSQL ServerのSystemErrorLogsテーブルに記録する。
- ファイルログやWindowsイベントログへの二重記録は行わない。
- DB接続障害等によってSystemErrorLogsへ書き込めない場合の代替ログは、費用対効果を考慮して初期実装には設けない。
- 利用状況はToolUsageLogsを期間・ツール・イベント種別・ユーザー等で集計する。
- 初期段階では集計専用テーブルを設けず、ToolUsageLogsから都度集計する。
- 集計条件に応じたインデックスは、ログの詳細項目と利用者との協議結果を踏まえて設計する。

## 7.9 共通API・共通メソッド

### ユーザー関連

```text
GET /api/users/me
GET /api/users/me/preferences
PUT /api/users/me/preferences
```

- ログインユーザー情報を取得する。
- 内部ユーザーIDと権限を取得する。
- ログインユーザーの通知設定を取得・更新する。

### ログ関連（Common.dll）

- Webツール利用履歴の登録にはHTTP APIを設けず、Common.dllが提供する非同期メソッドを使用する。
- 共通メソッドは少なくともツールID、イベント種別、処理結果を受け取り、認証情報から内部ユーザーIDを補完してToolUsageLogsへ登録する。
- Webツール起動は各Webツールの共通入口処理から、計算実行は各ツールの処理完了箇所から共通メソッドを呼び出す。
- ポータル・各Webツールのシステムエラーログを登録する。
- ツール開発者が一行程度のメソッド呼び出しで記録できるようにする。
- ログ記録の失敗によって、Webツールの計算処理そのものを失敗扱いにしない。
- デスクトップツールのダウンロード要求はポータル側で記録する。
- ポータルはWebツールの利用履歴を生成せず、ToolUsageLogsの参照・集計だけを担当する。
- ユーザーアクセスログはポータルの認証処理で記録する。
- デスクトップツールからログAPIを呼び出すことは求めない。

### 共通情報関連

- ツール情報を取得する。
- 共通設定を取得する。

## 7.10 共通ライブラリ

```text
SalesSupport.Common
├─ Authentication
├─ Logging
├─ TemporaryFiles
├─ UI
├─ Validation
└─ Contracts
```

- 共通ライブラリはローカルNuGetパッケージ等としてバージョンを付けて配布する。
- UIはRazor Class Library等を利用し、ヘッダー、フッター、ナビゲーション、Bootstrap設定、エラー画面、共通部品を統一する。
- UIは設定ファイルの環境区分に応じて、本番環境の青帯または開発環境の赤帯と環境名を表示する。
- UIは設定ファイルから本文テーマを取得し、環境を示す帯色とは独立して適用する。
- 初期開発では社内システム統一テーマだけを提供し、利用者ごとのテーマ設定は設けない。
- LoggingはAction Filterまたは簡単なメソッド呼び出しでツール利用履歴を記録できるようにする。
- ツール開発者が個別にログ送信処理を作り込まない構成とする。
- Loggingはアプリケーションから渡されたDB接続設定を使用し、接続文字列や認証情報をCommon.dllへ埋め込まない。
- Authenticationまたは共通の入口処理はツール状態を確認し、Webツールへの直接URLアクセスにもメンテナンス中、利用停止および非公開の制御を適用する。
- TemporaryFilesは一時ファイルの保存、読込、削除、期限切れ削除を提供する。
- 問い合わせ添付ファイルはTemporaryFilesで一時保存し、メールへ添付した後に削除する。
- TemporaryFilesの利用例を標準テンプレートまたはサンプルツールに含める。

---

# 8. 配置・運用

## 8.1 配布・配置方針

- デスクトップアプリケーションは管理画面からアップロード・公開する。
- Webツールとデスクトップツールのマニュアルは管理画面からアップロード・公開する。
- Webツール本体は`dotnet publish`済み成果物をサーバーへ手動配置する。
- Webツール本体を管理画面からアップロード・配備する機能は設けない。
- ポータル自身の更新もサーバーへ手動配置する。

## 8.2 手動リリース

- 初期段階ではCI/CDを利用しない。
- リリースに使用する.NET SDKのバージョンを定める。
- `restore`、`build`、`test`、`publish`の手順を文書化する。
- Common.dllを更新したツールは、参照パッケージのバージョンを更新してから`restore`、`build`、`test`、`publish`を実行する。
- 稼働中の`bin`フォルダーへCommon.dllだけを一括上書きする運用は行わない。
- 配布成果物と変更内容を保管する。
- 本番反映の担当者と承認方法を定める。
- Webツールの旧バージョンへの切り戻し手順を用意する。

## 8.3 サーバー環境確認チェックリスト

### OS・基本情報

- [ ] Windows Serverの製品名、バージョン、エディション、OSビルドを確認した
- [ ] OSがx64であることを確認した
- [ ] 対象OSが.NET 10の正式なサポート対象であることを確認した
- [ ] Windows Serverのサポート期限が運用予定期間を満たすことを確認した
- [ ] OSのセキュリティ更新を継続して適用できることを確認した
- [ ] CPU、メモリ、ディスク容量を確認した

### IIS・.NET

- [ ] Web Server（IIS）サーバーロールを利用できることを確認した
- [ ] IISのバージョンと現在の構成を確認した
- [ ] IIS有効化後に.NET 10 Hosting Bundleをインストールできることを確認した
- [ ] ASP.NET Core Module V2を利用できることを確認した
- [ ] .NET 10の月次パッチを継続して適用できることを確認した
- [ ] Hosting Bundleの導入・更新に必要な権限と承認手続きを確認した
- [ ] 導入・更新後にサーバーまたはIISサービスを再起動できることを確認した
- [ ] IISサイトとツールごとのアプリケーションプールを作成できることを確認した

### ネットワーク・セキュリティ

- [ ] DNS名とHTTPS用サーバー証明書を取得・更新できることを確認した
- [ ] 利用者端末からポータルへの通信経路を確認した
- [ ] SQL Serverへの接続経路とファイアウォール設定を確認した
- [ ] SMTPサーバーへの接続可否を確認した
- [ ] Windows認証採用時のIIS・Active Directory連携可否を確認した
- [ ] 機密情報を安全に保管できることを確認した
- [ ] 配置先、ログ、マニュアル、配布ファイルのアクセス権限を確認した

### 検証・運用

- [ ] 本番相当の構成で事前検証できることを確認した
- [ ] 本番環境と開発環境の設定ファイルに正しい環境区分が設定されていることを確認した
- [ ] 本番環境で青帯、開発環境で赤帯と環境名が表示されることを確認した
- [ ] 開発環境の本文テーマを開発用設定で切り替えて確認でき、赤い環境帯は固定表示されることを確認した
- [ ] ASP.NET Core 10アプリをIISで起動できることを確認した
- [ ] HTTPS、認証、SQL Server接続、メール送信を確認した
- [ ] アプリケーションログを参照できることを確認した
- [ ] サーバー側でファイル・DBバックアップを実施できることを確認した
- [ ] サーバー監視、ディスク監視、障害通知の方法を確認した
- [ ] 手動でのビルド、テスト、発行、本番反映手順を確認した

## 8.4 バックアップ運用

- ファイルバックアップとDBバックアップはサーバー側の運用機能で実施する。
- タスクスケジューラ等によるバックアップ運用を前提とする。
- ポータルおよび各ツールにはバックアップ機能を実装しない。
- システム側は、バックアップ対象となるDBとファイル格納先をサーバー管理者へ提示する。

## 8.5 サービス開始時のユーザー登録

- 初期ユーザーは、事前に確認した一覧を使用してSQLスクリプトから一括登録する。
- UserIdはSQL Server側でUUIDを生成する。
- ユーザーとユーザー設定を一つのトランザクションで登録する。
- 登録後に予定件数、メールアドレスの重複、有効な管理者数およびユーザー設定の作成件数を確認する。
- 初期登録後に追加するユーザーは、原則としてユーザー管理画面から登録する。

---

# 9. ツール開発標準

## 9.1 共通ルール

- ASP.NET Core、.NET、Bootstrapのバージョンを統一する。
- C#コーディング規約を統一する。
- ソリューション、プロジェクト、URLの命名規則を統一する。
- 画面レイアウト、ヘッダー、フッター、ナビゲーションを統一する。
- 社内システム統一テーマと、環境区分に応じた帯色・環境名の表示を共通UIで統一する。
- 独自CSSで背景色や文字色を固定せず、Bootstrapのテーマ機能とCSS変数を優先して使用する。
- 認証、権限取得、ログ、エラー処理を統一する。
- DBアクセス、API呼び出し、設定ファイル構成を統一する。
- テスト方式とリリース方式を統一する。

## 9.2 一時ファイル取扱い制約

- SalesSupport.Commonの一時ファイル機能を使用する。
- 一時ファイルはWeb公開・アプリケーション配置フォルダーの外へ保存する。
- ツールごとに保存領域を分離する。
- 利用者が指定したファイル名を物理ファイル名として直接使用しない。
- 一時ファイルIDまたはランダムなファイル名を使用する。
- 許可する拡張子と最大ファイルサイズをツールごとに定義する。
- 問い合わせ添付ファイルは、PDF、Excel、Word、CSV、テキスト、PNG、JPEGのいずれか一ファイル、最大10MBとする。
- 問い合わせ添付ファイルはメール送信に必要な間だけ一時保存し、メールへ直接添付する。
- 問い合わせ添付ファイルの内容、ファイル名および保存先はDBで管理しない。
- 一時保存領域に実行権限を付与しない。
- 正常・異常にかかわらず、処理終了時に一時ファイルを削除することを原則とする。
- 問い合わせ添付付きメールの送信失敗時だけは、再送方法と削除時期を今後決定する。
- 処理結果および帳票データを恒久保存しない。
- 残存した一時ファイルは起動時・定期削除の対象とする。
- ファイル内容と物理パスをログへ記録しない。

## 9.3 ログ実装制約

- デスクトップツールのダウンロード要求はポータル側で記録する。
- Webツール起動および計算実行は、各WebツールからSalesSupport.Commonのログ記録メソッドを呼び出して記録する。
- Webツール利用履歴の登録用HTTP APIは設けない。
- ポータルはWebツール利用履歴の参照・集計を担当し、Webツールの代わりに履歴を生成しない。
- デスクトップツールからログAPIを呼び出さない。
- 操作対象データや計算結果をログへ保存しない。
- 共通のイベント種別、処理結果、相関IDの形式を使用する。

## 9.4 サンプルツール

- 標準的な画面、Controller、Serviceおよびデータアクセス構成を示す。
- Repository層を採用するかは今後の決定に従い、決定前は必須構成として固定しない。
- 認証情報と内部ユーザーIDの取得例を含める。
- 共通DLLによるWebツール起動・計算実行履歴とエラーログの記録例を含める。
- 一時ファイルのアップロード、処理、削除例を含める。

---

# 10. 今後決定が必要な事項

1. 認証方式
   - ポータル独自認証
   - Windows認証
   - Microsoft Entra ID等
   - Windows認証またはEntra ID採用時の外部識別子とUsers.UserIdの紐付け方法

2. 独自認証採用時のパスワード管理方式

3. ポータルと各Webツール間の認証情報共有方式

4. IIS上のアプリケーション配置方式

5. 共通APIの詳細仕様

6. 共通ライブラリの配布範囲とバージョン管理方針

7. Repository層の採用方針
   - EF CoreのDbContextをServiceから直接利用するか
   - Repository層を全ツールへ設けるか、複雑なデータアクセスだけに限定するか

8. DBのスキーマ構成
   - すべてdboとするか
   - ポータル・共通領域・各ツールでスキーマを分けるか
   - 分割によって得られる権限管理・名前整理上の効果が運用コストに見合うか

9. メール送信方式およびサーバー側の容量制限
   - 問い合わせ画面では最大10MBの添付を受け付ける
   - IISの要求サイズ上限と、メール形式による容量増加を含めたSMTP側の上限を確認する

10. 問い合わせ添付ファイルの異常時処理
   - メール送信失敗時の再送方法
   - 一時ファイルをいつ削除するか
   - 問い合わせレコードだけが残る場合の管理者への案内方法

11. デスクトップツールからWebツールへの移行運用
   - 旧デスクトップツールの停止・非公開時期
   - お気に入り、利用履歴、お知らせ等を新しいToolIdへ引き継ぐか

12. ツール開発用標準テンプレートの詳細

13. ログ取得・利用状況集計の詳細
   - 取得するイベント種別
   - 利用ユーザーとして数える操作の範囲
   - 月別および期間全体の集計方法
   - 画面へ表示する集計項目
   - TSVへ出力する項目と並び順
   - 利用者との確認・合意

14. サーバー監視・障害調査方式

15. Visual Studio Codeで使用するC#拡張機能とライセンス

---

# 11. 基本方針まとめ

### 利用者

- 一つのポータルからWebツールとデスクトップツールを利用する。
- お知らせ、マニュアル、通知を同じ入口から確認する。

### ツール開発者

- 各ツールを独立したソリューションとして開発する。
- 共通API、共通ライブラリ、標準テンプレートを利用する。
- ツール固有の業務機能の開発に集中する。

### ポータル管理者

- ユーザー、ツール、配布ファイル、マニュアル、お知らせ、問い合わせを管理する。
- ログと利用状況を確認する。
- Webツール本体はサーバーへ手動配備する。

この責任分界により、約20個のツールを複数開発者で並行して開発・保守しながら、利用者からは一つの営業支援サイトとして見える構成を目指す。
