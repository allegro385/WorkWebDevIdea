# 営業支援ポータル システム構成・開発標準

[資料一覧へ戻る](README.md)

採用技術、ポータルと各ツールの責任分界、および開発者が共通で守る実装方式を定義する。状態ごとの業務ルールやテーブル定義は重複掲載せず、関連資料を参照する。

## 目次

- [1. 技術・開発環境](#technology)
- [2. 全体構成・責任分界](#architecture)
- [3. URL構成案](#urls)
- [4. アプリケーション・ソリューション構成](#application-structure)
- [5. 認証構成](#authentication)
- [6. 共通利用制御の実装](#access-implementation)
- [7. 共通ライブラリの構成](#common-library)
- [8. 共通API・共通情報取得](#common-api)
- [9. ログ実装](#logging)
- [10. 環境設定・共通UI](#ui)
- [11. 一時ファイル取扱い制約](#temporary-files)
- [12. 開発共通ルール・サンプル](#development-standard)

<a id="technology"></a>

## 1. 技術・開発環境

| 領域 | 技術 |
| --- | --- |
| 開発エディター | Visual Studio Code |
| プラットフォーム | .NET 10 LTS |
| Webフレームワーク | ASP.NET Core 10 MVC |
| 開発言語 | C# |
| View | Razor |
| UI | Bootstrap |
| クライアント処理 | JavaScript |
| DB | SQL Server |
| Webサーバー | IIS |

### 開発上の留意事項

- 新規開発では.NET Frameworkではなく.NET 10 LTSを使用する。
- 社内で使用中のASP.NET、VB.NET、.NET Framework 4.7とは構成や開発方式が異なることに留意する。
- 開発開始前に標準テンプレート、サンプルツール、コーディング規約を用意する。
- Visual Studio Codeで使用するC#拡張機能の機能とライセンスを確認する。
- .NET SDKのバージョンを統一する。

<a id="architecture"></a>

## 2. 全体構成・責任分界

```text
営業支援ポータル
│
├─ Portal
│   ├─ 認証・ユーザー管理
│   ├─ ツール・お知らせ管理
│   ├─ サイト公開状態管理
│   ├─ お気に入り・通知
│   ├─ ログTSV出力
│   ├─ 問い合わせ・問い合わせ管理
│   └─ 共通API・共通UI
│
├─ Tool001
├─ Tool002
├─ Tool003
│   ...
└─ Tool020
```

### ポータル側の責務

- 認証・認可
- ユーザー管理
- ツール情報・状態管理
- サイト全体の公開状態・入場制限時の案内文の管理
- サイト公開状態、ツール状態およびユーザー権限に基づく共通利用制御
- デスクトップアプリケーション配布
- マニュアル配布
- お気に入り
- システム・ツールのお知らせ
- メール通知
- 問い合わせ受付・問い合わせ管理
- デスクトップツールのダウンロード要求履歴の記録
- ツール別利用者数の集計およびログのTSV出力
- 共通UI・共通API・共通ライブラリ
- ツールへの遷移

### 各ツール側の責務

- ツール固有画面
- 業務ロジック・計算処理
- 業務データの登録、更新、検索
- 帳票作成・ダウンロード
- ファイル生成
- ツール固有の入力チェックとエラー処理
- 共通DLLのメソッドを使用したWebツール起動・計算実行履歴の登録
- 共通の入口処理によるサイト公開状態・ツール状態・ユーザー権限の確認と直接URLアクセスの制御
- 使用した一時ファイルの削除

<a id="urls"></a>

## 3. URL構成案

```text
https://sales-support/
├─ /                       ポータルトップ
├─ /tools                  ツール一覧
├─ /tools/favorites        お気に入りツール
├─ /tools/T001             Tool001詳細
├─ /tools/T001/app         Tool001 Webアプリ
├─ /support                サポート
├─ /support/inquiries      問い合わせ・ご意見
├─ /settings               個人設定
├─ /admin                  管理機能
├─ /admin/site             サイト公開状態管理
├─ /admin/tools            ツール選択
├─ /admin/tools/T001/basic ツール基本情報
├─ /admin/tools/T001/resources 提供内容
├─ /admin/tools/T001/versions  バージョン
├─ /admin/tools/T001/notices   ツールのお知らせ
└─ /admin/inquiries        問い合わせ管理
```

内部的に別Webアプリケーションであっても、利用者からは同一ドメイン配下の一つのサイトとして見える構成を目指す。

<a id="application-structure"></a>

## 4. アプリケーション・ソリューション構成

```text
Controller
    ↓
Service
    ↓
データアクセス
    ↓
SQL Server
```

- ControllerはHTTPリクエスト、画面遷移、入力受付を担当する。
- Serviceは計算処理等の業務ロジックを担当する。
- データアクセスにはEF CoreのDbContextをServiceから利用する構成、またはRepositoryを介する構成を選択できる。
- Repository層を全ツールへ必須とするかは、採用するデータアクセス技術と標準化の費用対効果を踏まえて今後決定する。
- Dependency Injectionを利用して各層を疎結合にする。

### ソリューション構成

```text
SalesSupport.Portal.sln
├─ SalesSupport.Portal
├─ SalesSupport.Api
└─ Portal関連プロジェクト

SalesSupport.Tool001.sln
├─ SalesSupport.Tool001.Web
├─ SalesSupport.Tool001.Application
└─ SalesSupport.Tool001.Tests

SalesSupport.Tool002.sln
├─ SalesSupport.Tool002.Web
├─ SalesSupport.Tool002.Application
└─ SalesSupport.Tool002.Tests
```

- ポータルと各ツールは個別のソリューションとして管理する。
- 複数開発者が互いのツールへの影響を抑えて並行開発できる構成とする。

Repository層の採用方針は[検討事項](06_検討事項.md#repository)を参照する。

<a id="authentication"></a>

## 5. 認証構成

- ASP.NET Core IdentityとCookie認証を使用する。
- ユーザークラスは`IdentityUser<Guid>`を継承した`ApplicationUser`とする。
- ロール系テーブルを使用せず、`IdentityUserContext<ApplicationUser, Guid>`を基礎とする。
- ログインIDにはメールアドレスを使用し、IdentityのUserNameにもメールアドレスを設定する。
- 表示用の氏名は独自列のDisplayNameへ保持する。
- 権限は独自列のRoleCodeでUSERまたはADMINを管理し、認証Cookieのロールクレームへ設定する。
- RoleCodeまたはIsActiveを変更した場合はSecurityStampを更新し、変更前の認証Cookieを継続利用できないようにする。
- ログイン、ログアウト、初回パスワード設定、パスワード変更およびパスワード再設定の画面はポータル側に設ける。
- 各Webツールはパスワードを取り扱わず、ポータルと共有する認証Cookieを検証する。
- ポータルと各Webツールで、認証スキーム、Cookie名、Cookieパス、Data Protectionのアプリケーション名およびキーストレージを共有する。
- Data ProtectionキーはIISの各対象アプリケーションプールから参照できる共通領域へ保存し、必要なアクセス権限だけを付与する。
- ユーザー情報の登録・更新、メールアドレス変更、ロック解除およびパスワード再設定処理にはUserManagerを使用する。
- パスワードのハッシュ化・検証・ロック・再設定はIdentity標準機能を使用し、独自の暗号処理は実装しない。
- パスワード変更時はSecurityStampを更新し、既存セッションを無効化する。
- Identity管理テーブルの同時更新はConcurrencyStampで検出する。
- 管理者は利用者の現在のパスワードおよびパスワードハッシュを表示・取得できない。
- パスワードとパスワードハッシュを画面・メール本文・ログへ出力しない。
- 初回設定・再設定トークンは、設定リンクとしての受渡し以外に画面・メール本文へ表示せず、ログにも記録しない。
- 認証CookieはSecureおよびHttpOnlyを有効にし、有効期間とSameSite属性は詳細設計で定める。
- 初回設定・再設定トークンには有効期限を設け、一回の設定完了で無効化する。
- Data Protectionキーは永続化し、保存時の保護とアクセス権限を設定する。

画面動作は[ログイン・パスワード関連画面](02_画面・機能設計.md#login)、カラムと採番は[ユーザーデータ](04_DB設計.md#users)、未確定パラメーターは[検討事項](06_検討事項.md#identity)を参照する。

<a id="access-implementation"></a>

## 6. 共通利用制御の実装

- 判定ルールは[画面・機能設計の共通利用制御](02_画面・機能設計.md#access-control)を参照し、画面・ツールごとに独自の利用可否表を実装しない。
- 認証情報とユーザーの有効状態・権限を検証した後、SystemSettingsのサイト公開状態を確認し、入場可能な場合だけ個別ツールの状態を確認する。
- 各Webツールに共通処理を必ず組み込み、ポータルを経由しない直接URL、業務APIおよび保護対象ファイルの取得にも適用する。
- ポータルのアプリケーションダウンロード要求およびデータリンクの提供にも同じ判定を適用する。
- 未認証で必要な経路の例外は画面・機能設計に従い、管理者向けの独自バイパスは設けない。
- 状態変更はログイン済みユーザーの次のリクエストにも反映し、既に実行中の処理の強制中断は行わない。
- 公開状態・ツール状態を取得できない場合、または未定義のコード値の場合は、制限を解除して処理を継続しない。
- DB上の表示名称や色ではなく、定義済みコード値と検証済みの権限を使って判定する。

<a id="common-library"></a>

## 7. 共通ライブラリの構成

```text
SalesSupport.Common
├─ Authentication
├─ Logging
├─ TemporaryFiles
├─ UI
├─ Validation
└─ Contracts
```

| 構成要素 | 役割 |
| --- | --- |
| Authentication | 認証情報の取得・検証と共通利用制御 |
| Logging | 利用履歴・アクセスログ・エラーログの共通記録 |
| TemporaryFiles | 一時ファイルの保存・読込・削除・期限切れ削除 |
| UI | ヘッダー、ナビゲーション、テーマ、状態バッジ、共通エラー画面 |
| Validation | 共通入力検証 |
| Contracts | 共通API・共通メソッドの受渡し定義 |

- CommonはローカルNuGetパッケージ等でバージョン管理し、各ソリューションから明示したバージョンを参照する。
- DB接続設定はアプリケーションからDependency Injection等で渡し、Common.dllへ接続文字列や認証情報を埋め込まない。
- 配布時の更新・検証・発行手順は[手動リリース](05_導入・運用.md#release)に従う。
- 各機能の利用例を標準テンプレートまたはサンプルツールに含める。

<a id="common-api"></a>

## 8. 共通API・共通情報取得

### ユーザー関連HTTP API

```text
GET /api/users/me
GET /api/users/me/preferences
PUT /api/users/me/preferences
```

- ログインユーザー情報を取得する。
- 内部ユーザーIDと権限を取得する。
- ログインユーザーの通知設定を取得・更新する。

### 共通情報関連

- ツール情報を取得する。
- 共通設定を取得する。
- CodeMasterから指定したコード区分の選択肢、表示名称、並び順および表示色を取得する共通メソッドを提供する。
- SystemSettingsから現在のサイト公開状態と入場制限時の案内文を取得する共通メソッドを提供する。

<a id="logging"></a>

## 9. ログ実装

- Webツール利用履歴の登録にはHTTP APIを設けず、Common.dllが提供する非同期メソッドを使用する。
- 共通メソッドは少なくともツールID、イベント種別、処理結果を受け取り、認証情報から内部ユーザーIDを補完してToolUsageLogsへ登録する。
- Webツール起動は各Webツールの共通入口処理から、計算実行は各ツールの処理完了箇所から共通メソッドを呼び出す。
- ポータル・各Webツールのシステムエラーログを登録する。
- ツール開発者が一行程度のメソッド呼び出しで記録できるようにする。
- ログ記録の失敗によって、Webツールの計算処理そのものを失敗扱いにしない。
- デスクトップツールのダウンロード要求はポータル側で記録する。
- ポータルはWebツールの利用履歴を生成せず、ToolUsageLogsの参照・集計だけを担当する。
- ユーザーアクセスログは、ポータルの認証処理およびポータル・各Webツールの共通利用制御処理で記録する。
- デスクトップツールからログAPIを呼び出すことは求めない。
- Action Filterまたは簡単な共通メソッド呼び出しで記録できる構成とし、ツール開発者が個別にログ送信処理を作り込まない。
- 共通のイベント種別、処理結果、相関IDの形式を使用する。
- 操作対象データや計算結果をログへ保存しない。

記録イベントとTSV出力は[ログTSV出力](02_画面・機能設計.md#logs-usage)、保存先と集計元は[ログテーブル](04_DB設計.md#logs)、保持期間は[要件定義](01_要件定義.md#retention)を参照する。

<a id="ui"></a>

## 10. 環境設定・共通UI

- 環境識別・本文テーマの見た目は[要件定義](01_要件定義.md#theme)を参照する。
- 環境区分は設定ファイルのProductionまたはDevelopmentとし、共通設定クラスから参照する。
- 環境区分は利用者が画面から変更できないようにする。
- 本文には社内システム統一テーマを固定して適用し、テーマ切替用の設定は設けない。
- 環境判定、固定テーマの適用および帯表示は共通UIへ実装し、ポータルと各Webツールで再利用する。
- 利用者別テーマ選択とダークモードは対象外とする。
- 本番環境は青、開発環境は赤の帯を表示し、帯の中や周辺に環境名を示す文字は表示しない。
- 独自CSSで背景色や文字色を固定せず、Bootstrapのテーマ機能とCSS変数を優先して使用する。
- UIはRazor Class Library等を利用し、ヘッダー、フッター、ナビゲーション、Bootstrap設定、エラー画面および共通部品を統一する。
- CodeMasterのCodeNameとColorCodeを使用して状態等の共通バッジを描画し、各画面で名称や色を重複定義しない。
- ColorCodeはバッジ背景色に使用し、文字色は白を基本とする。未設定の場合は画面側の標準表示を使用する。
- 白文字を読み取りやすい背景色を登録し、色だけでなく表示名称も併記する。
- コードごとの色は、環境帯や本文テーマとは分離する。
- CodeMasterの取得は共通メソッドを使用し、値の形式と初期設定案は[コードマスタ](04_DB設計.md#code-master)を参照する。

<a id="temporary-files"></a>

## 11. 一時ファイル取扱い制約

- SalesSupport.Commonの一時ファイル機能を使用する。
- 一時ファイルはWeb公開・アプリケーション配置フォルダーの外へ保存する。
- ツールごとに保存領域を分離する。
- 利用者が指定したファイル名を物理ファイル名として直接使用しない。
- 一時ファイルIDまたはランダムなファイル名を使用する。
- 許可する拡張子と最大ファイルサイズをツールごとに定義する。
- 問い合わせ添付ファイルはメール送信に必要な間だけ一時保存し、メールへ直接添付する。
- 一時保存領域に実行権限を付与しない。
- 正常・異常にかかわらず、処理終了時に一時ファイルを削除する。
- 問い合わせメールは一回だけ送信を試行し、失敗時の再送を目的として一時ファイルを保持しない。
- 処理結果および帳票データを恒久保存しない。
- 残存した一時ファイルは起動時・定期削除の対象とする。
- ファイル内容と物理パスをログへ記録しない。
- TemporaryFilesは一時ファイルの保存、読込、削除および期限切れ削除を提供する。
- 問い合わせ添付の種類・容量・保存方針は[問い合わせ仕様](02_画面・機能設計.md#inquiries)と[問い合わせデータ](04_DB設計.md#inquiries)を参照する。

<a id="development-standard"></a>

## 12. 開発共通ルール・サンプル

- ASP.NET Core、.NET、Bootstrapのバージョンを統一する。
- C#コーディング規約を統一する。
- ソリューション、プロジェクト、URLの命名規則を統一する。
- 認証、権限取得、ログ、エラー処理およびUIには本資料の共通処理を使用する。
- DBアクセス、API呼び出し、設定ファイル構成を統一する。
- テスト方式とリリース方式を統一する。

### サンプルツール

- 標準的な画面、Controller、Serviceおよびデータアクセス構成を示す。
- Repository層を採用するかは今後の決定に従い、決定前は必須構成として固定しない。
- 認証情報と内部ユーザーIDの取得例を含める。
- サイト公開状態・ツール状態・管理者権限による共通利用制御と、CodeMasterによる状態表示の例を含める。
- 共通DLLによるWebツール起動・計算実行履歴とエラーログの記録例を含める。
- 一時ファイルのアップロード、処理、削除例を含める。

共通エラー表示の要件は[要件定義](01_要件定義.md#nonfunctional-requirements)、標準テンプレートの未決定部分は[検討事項](06_検討事項.md#template)を参照する。
