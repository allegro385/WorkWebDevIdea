# 営業支援ポータル 要件・基本構成案

## 1. システム概要

### 1.1 目的

既存の営業支援用デスクトップツール群を集約し、Webツールおよびデスクトップツールを一元的に利用できる「営業支援ポータル」を構築する。

各ツールは独立したソリューションとして開発可能とし、複数の開発者が並行してツール開発・保守を行える構成とする。

利用者からは、各ツールが独立したシステムであることを極力意識させず、一つの営業支援サイトとして利用できることを目指す。

### 1.2 想定利用規模

| 項目 | 想定 |
|---|---|
| 登録ユーザー数 | 約200名 |
| 同時利用ユーザー数 | 約5名 |
| 初期ツール数 | 約20ツール |
| ツール種別 | Webツール / デスクトップツール |
| Web基盤 | ASP.NET |
| UI | Bootstrap |
| DB | SQL Server |

---

# 2. システム全体構成

```text
営業支援ポータル
│
├─ トップ
│   ├─ お知らせ
│   ├─ ツール一覧
│   ├─ サポート
│   └─ 管理
│
├─ ポータル機能
│   ├─ 認証・認可
│   ├─ ユーザー管理
│   ├─ お気に入り管理
│   ├─ お知らせ管理
│   ├─ メール通知
│   └─ ツール管理
│
├─ Tool001
│   └─ 個別ソリューション
│
├─ Tool002
│   └─ 個別ソリューション
│
├─ Tool003
│   └─ 個別ソリューション
│
│   ...
│
└─ Tool020
    └─ 個別ソリューション
```

ポータルと各ツールの責務を分離し、各ツール固有の業務処理はツール側へ実装する。

---

# 3. ポータルトップ画面

## 3.1 基本レイアウト

トップ画面は、大きく以下の3エリアに分ける。

```text
┌─────────────────────────────────────┐
│ 営業支援ポータル                     │
│                          ユーザー情報 │
├─────────────────────────────────────┤
│                                     │
│ ■ お知らせ                          │
│                                     │
│  2026/XX/XX システムのお知らせ       │
│  2026/XX/XX メンテナンスのお知らせ   │
│                                     │
├────────────┬────────────┬────────────┤
│ ツール     │ サポート   │ 管理       │
│            │            │ ※管理者のみ│
│ ツール一覧 │ マニュアル │ 非公開設定 │
│            │ FAQ        │ メンテナンス│
│            │ 個人設定   │ ユーザー管理│
│            │ 問い合わせ │            │
└────────────┴────────────┴────────────┘
```

### お知らせ

トップ画面上部にシステム全体のお知らせを表示する。

独立したメニューとしてではなく、ログイン後すぐに確認できる情報としてトップ画面内に配置する。

### ツール

以下へのリンクを配置する。

- ツール一覧

### サポート

以下へのリンクを配置する。

- 利用マニュアル・FAQ
- 個人設定
- 問い合わせ

### 管理

システム管理者のみ表示する。

以下へのリンクを配置する。

- ツール非公開設定
- ツールメンテナンス
- ユーザー管理

---

# 4. 画面一覧

| ID | 画面 | 利用者 |
|---|---|---|
| P001 | ログイン | 全ユーザー |
| P002 | ポータルトップ | 全ユーザー |
| P003 | ツール一覧 | 全ユーザー |
| P004 | ツール詳細 | 全ユーザー |
| P005 | 利用マニュアル・FAQ | 全ユーザー |
| P006 | 個人設定 | 全ユーザー |
| P007 | 問い合わせ | 全ユーザー |
| A001 | ツール非公開設定 | システム管理者 |
| A002 | ツールメンテナンス | システム管理者 |
| A003 | ユーザー管理 | システム管理者 |

※各Webツール内部の画面は、本ポータルの画面一覧には含めず、各ツール側で管理する。

---

# 5. 機能要件

## 5.1 認証

ユーザーIDにはメールアドレスを使用する。

ログイン後、ユーザーの権限に応じて利用可能な機能を制御する。

### ユーザー種別

初期段階では以下を想定する。

- 一般ユーザー
- システム管理者

必要に応じて、将来的にツール管理者等の権限追加を検討する。

---

## 5.2 ツール一覧

登録されているツールを一覧表示する。

Webツールとデスクトップツールを別画面には分けず、同一のツール一覧上に表示する。

ツール数は約20個を想定しているため、初期リリースではキーワード検索機能を実装しない。

### 表示情報例

- ツール名
- ツール概要
- ツール種別
- 更新日
- お気に入り状態
- 公開状態

ツールを選択すると、Webツール・デスクトップツールのどちらの場合でも、最初にポータル側の「ツール詳細画面」を表示する。

---

## 5.3 お気に入り

ツール一覧から任意のツールをお気に入りとして登録・解除できる。

ツール一覧では、

- すべてのツール
- お気に入りのみ

を切り替えて表示できる。

お気に入り情報はユーザー単位で保持する。

---

# 6. ツール詳細

## 6.1 概要

Webツールとデスクトップツールの入口を統一するため、各ツールについてポータル側に共通のツール詳細画面を用意する。

```text
ツール一覧
   │
   ▼
ツール詳細
   │
   ├─ ツールのお知らせ
   ├─ マニュアル
   │
   ├─ Webツールの場合
   │      └─ Webツールを開く
   │
   └─ デスクトップツールの場合
          └─ デスクトップツールをダウンロード
```

## 6.2 ツールのお知らせ

ツール固有のお知らせを表示する。

想定する内容は以下とする。

- バグ・不具合情報
- メンテナンス情報
- アップデート情報
- 新機能
- 利用上の注意

---

## 6.3 マニュアル

ツールの利用マニュアルをダウンロードできるようにする。

Webツール・デスクトップツールの両方を対象とする。

---

## 6.4 Webツール

Webツールの場合、「Webツールを開く」ボタンを表示する。

選択すると、該当するWebツールへ遷移する。

```text
ポータル

/tools/T001
      │
      ▼
Webツール

/tools/T001/app/...
```

具体的なURL構成についてはシステム設計時に決定する。

---

## 6.5 デスクトップツール

デスクトップツールの場合、ツール詳細画面からアプリケーションをダウンロードできるようにする。

必要に応じて以下の情報も管理する。

- バージョン
- 公開日
- 更新内容
- 対応環境
- インストール方法
- ファイル

---

# 7. お知らせ・メール通知

## 7.1 システムのお知らせ

ポータル全体に関係する情報をトップ画面へ掲載する。

お知らせが新規登録・更新された場合、メール通知を希望しているユーザーへ通知する。

---

## 7.2 ツールのお知らせ

ツールごとに以下のような情報を掲載する。

- 障害
- バグ
- アップデート
- メンテナンス
- その他重要情報

ユーザーがお気に入り登録しているツールのお知らせが新規登録・更新された場合、対象ユーザーへメールを送信する。

ただし、個人設定で通知を無効にしているユーザーには送信しない。

---

# 8. 個人設定

個人設定画面では、ユーザー自身の設定を変更できるようにする。

## 8.1 通知設定

以下を個別にON/OFFできる。

| 設定 | 内容 |
|---|---|
| システム通知 | ポータル全体のお知らせメールを受信する |
| お気に入りツール通知 | お気に入りツールのお知らせメールを受信する |

例えば以下の設定を可能とする。

```text
システムのお知らせ
[✓] メールを受信する

お気に入りツールのお知らせ
[✓] メールを受信する
```

---

## 8.2 パスワード変更

個人設定画面からログインパスワードを変更できるようにする。

※将来的にWindows認証やMicrosoft Entra ID等の外部認証基盤を採用する場合は、ポータル独自のパスワード変更機能を持たせるか再検討する。

---

# 9. 管理機能

管理機能はシステム管理者のみ利用可能とする。

## 9.1 ツール非公開設定

ツール単位で公開・非公開を設定できるようにする。

非公開に設定されたツールは、一般ユーザーのツール一覧には表示しない。

---

## 9.2 ツールメンテナンス

ツールに関するポータル側の情報を管理する。

想定する管理項目は以下とする。

- ツールID
- ツール名
- ツール概要
- ツール種別
- WebツールURL
- デスクトップツール情報
- マニュアル
- バージョン
- 公開状態
- ツールのお知らせ

---

## 9.3 ユーザー管理

以下のユーザー情報を管理する。

- メールアドレス
- ユーザー名
- 権限
- アカウント有効・無効
- その他必要なユーザー情報

---

# 10. サポート

## 10.1 利用マニュアル・FAQ

ポータル自体の操作方法および共通FAQを掲載する。

各ツール固有のマニュアルについては、各ツールの詳細画面から参照可能とする。

---

## 10.2 問い合わせ

問い合わせ方法および問い合わせ先を表示する。

将来的に問い合わせフォーム等を実装する場合は別途検討する。

---

# 11. ポータルとツールの責任分界

## 11.1 ポータル側

ポータルは、営業支援ツールを利用するための共通基盤を担当する。

主な責務は以下とする。

- 認証
- 認可
- ユーザー管理
- ツール管理
- お気に入り
- システムのお知らせ
- ツールのお知らせ
- メール通知
- 共通UI
- 共通API
- ツールへの遷移

---

## 11.2 各ツール側

各ツール固有の業務処理については、各ツール側で実装する。

原則として以下をツール側の責務とする。

- ツール固有画面
- 業務ロジック
- 計算処理
- DBへの業務データ登録・更新
- 業務データ検索
- 帳票作成
- 帳票ダウンロード
- ファイル生成
- ツール固有情報の更新処理
- ツール固有入力チェック
- ツール固有エラー処理

---

# 12. 共通API

## 12.1 方針

各ツールを独立したソリューションとして開発する一方、各開発者が同じ処理を繰り返し実装することを防ぐため、ポータル共通基盤に共通APIを用意する。

```text
Tool001 ─┐
Tool002 ─┼──→ 共通API ──→ 共通機能
Tool003 ─┤
   ：    │
Tool020 ─┘
```

## 12.2 共通API候補

詳細については設計工程で決定するが、以下を候補とする。

### ユーザー関連

```text
GET /api/users/me
```

- ログインユーザー情報取得
- ユーザーID取得
- 権限取得

### ログ関連

- 操作ログ登録
- エラーログ登録

### 共通情報関連

- ツール情報取得
- 共通設定取得

その他、複数ツールで同一実装が発生した機能については、共通化の妥当性を確認したうえで追加する。

---

# 13. DB構成

## 13.1 基本方針

SQL Server上に本システム専用の業務DBを用意する。

ポータルおよび各ツールからは、専用のDBユーザーを使用して接続する。

```text
SQL Server
   │
   └─ 営業支援システム専用DB
         ▲
         │
    専用DBユーザー
         │
   ┌─────┴─────┐
   │           │
Portal       各Tool
```

ポータルおよび各ツールから、営業支援システムに不要な他システムのDBへ直接アクセスさせない。

## 13.2 アクセス権限

DBユーザーには必要最小限の権限のみを付与する。

ツールごとにアクセス可能なテーブル・スキーマを分離するかについては、詳細設計時に検討する。

---

# 14. ソリューション構成

## 14.1 基本方針

各ツールは個別のソリューションとして管理する。

```text
SalesSupport.Portal.sln
│
├─ SalesSupport.Portal
├─ SalesSupport.Api
└─ Portal関連プロジェクト


SalesSupport.Tool001.sln
│
├─ SalesSupport.Tool001.Web
├─ SalesSupport.Tool001.Application
└─ SalesSupport.Tool001.Tests


SalesSupport.Tool002.sln
│
├─ SalesSupport.Tool002.Web
├─ SalesSupport.Tool002.Application
└─ SalesSupport.Tool002.Tests

                ：
```

これにより、複数のツール開発者が互いのツールへの影響を抑えながら並行開発できるようにする。

---

# 15. 共通ライブラリ

各ツールで共通利用する機能については、共通ライブラリ化を検討する。

候補は以下とする。

```text
SalesSupport.Common
│
├─ Authentication
├─ Logging
├─ UI
├─ Validation
└─ Contracts
```

共通ライブラリについてはNuGetパッケージ等による配布を検討する。

特にUIについては、Razor Class Library等を利用して、

- ヘッダー
- フッター
- ナビゲーション
- Bootstrap設定
- エラー画面
- 共通部品

などを統一する。

---

# 16. Webアプリケーション構成

基本技術構成は以下を想定する。

| 領域 | 技術 |
|---|---|
| Webフレームワーク | ASP.NET Core MVC |
| バックエンド | C# |
| View | Razor |
| UI | Bootstrap |
| クライアント処理 | JavaScript |
| DB | SQL Server |

各ツールについては以下の基本構成を推奨する。

```text
Controller
    │
    ▼
Service
    │
    ▼
Repository
    │
    ▼
SQL Server
```

### Controller

HTTPリクエスト、画面遷移、入力受付を担当する。

### Service

計算処理等の業務ロジックを担当する。

### Repository

SQL Serverへのデータアクセスを担当する。

Dependency Injectionを利用し、各層の依存関係を疎結合にする。

---

# 17. URL構成案

利用者からは一つのWebサイトとして見える構成とする。

```text
https://sales-support/

├─ /
│   └─ ポータルトップ
│
├─ /tools
│   └─ ツール一覧
│
├─ /tools/T001
│   └─ Tool001詳細
│
├─ /tools/T001/app
│   └─ Tool001 Webアプリ
│
├─ /tools/T002
│   └─ Tool002詳細
│
├─ /tools/T002/app
│   └─ Tool002 Webアプリ
│
├─ /support
│
├─ /settings
│
└─ /admin
```

内部的に別ソリューション・別Webアプリケーションであっても、利用者からは同一ドメイン配下の一つのサイトとして利用できる構成を目指す。

---

# 18. 非機能要件

## 18.1 セキュリティ

- HTTPSを使用する。
- 認証されていないユーザーからのアクセスを制限する。
- 管理機能はシステム管理者のみ利用可能とする。
- DB接続には専用DBユーザーを使用する。
- DBユーザーには必要最小限の権限のみ付与する。
- 接続文字列やパスワード等の機密情報をソースコードへ直接記述しない。
- SQLインジェクション対策を実施する。
- XSS対策を実施する。
- CSRF対策を実施する。

## 18.2 保守性

- ツール単位でソリューションを分割する。
- ツール単位で改修・テスト・リリースできる構成を目指す。
- 共通処理は共通APIまたは共通ライブラリとして提供する。
- 各ツールの実装方式を可能な限り統一する。
- ツール開発用の標準テンプレートを用意する。

## 18.3 拡張性

初期ツール数は約20個とするが、将来的なツール追加を考慮する。

新しいツールについて、

```text
ツール開発
   ↓
ポータルへツール情報登録
   ↓
権限・URL等を設定
   ↓
公開
```

という流れで追加でき、既存ポータルの大規模改修を必要としない構成とする。

## 18.4 可用性・性能

想定規模は以下とする。

```text
登録ユーザー：約200名
同時利用：約5名
```

大規模な分散システムは前提とせず、保守性・運用性を優先した構成とする。

ただし、各ツールで長時間の計算処理、大量データ処理、大容量帳票生成等を行う場合は、ツール単位で別途性能要件を定義する。

---

# 19. ツール開発標準

各ツール開発者が個別ソリューションを作成する場合でも、サイト全体として統一されたシステムとなるよう、ツール開発標準を別途策定する。

最低限、以下を統一する。

- ASP.NET Coreのバージョン
- .NETのバージョン
- Bootstrapのバージョン
- C#コーディング規約
- ソリューション・プロジェクト命名規則
- URL命名規則
- 画面レイアウト
- ヘッダー・フッター
- ナビゲーション
- 認証方式
- 権限取得方式
- ログ形式
- エラー処理
- DBアクセス方式
- API呼び出し方式
- 設定ファイル構成
- テスト方式
- リリース方式

---

# 20. 今後決定が必要な事項

現時点では以下を未決事項とし、基本設計・詳細設計で決定する。

1. 認証方式
   - ポータル独自認証
   - Windows認証
   - Microsoft Entra ID等

2. パスワード管理方式
   - 独自認証を採用する場合のハッシュ化・ロックアウト・リセット方式

3. ポータルと各Webツール間の認証情報共有方式

4. IIS等でのアプリケーション配置方式

5. 共通APIの詳細仕様

6. 共通ライブラリの範囲

7. DBのスキーマ分割方式

8. DBユーザーをポータル・全ツール共通とするか、ツール単位で分割するか

9. デスクトップツールの配布方式

10. メール送信方式

11. お知らせの「更新」と判定する条件

12. メール通知を即時送信するか、一定間隔でまとめて送信するか

13. ツールごとのアクセス権限管理が必要か

14. ツール開発用標準テンプレート

15. 共通API・共通ライブラリのバージョン管理方針

16. ログ・監視・障害調査方式

17. バックアップ・リストア方式

18. 開発・テスト・本番環境の構成

---

# 21. 基本設計方針まとめ

本営業支援ポータルでは、以下を基本方針とする。

**利用者視点**

> Webツール・デスクトップツールを意識せず、一つの営業支援ポータルから必要なツールを利用できる。

**ツール開発者視点**

> 各ツールを独立したソリューションとして開発し、共通API・共通ライブラリを利用することで、ツール固有機能の開発に集中できる。

**ポータル設計者視点**

> 認証・ユーザー・ツール管理・お気に入り・お知らせ・通知・共通UI等を統括し、各ツールの業務ロジックには極力依存しない。

この責任分界により、約20個のツールを複数開発者で並行して開発・保守しながら、利用者からは一つのWebサイトとして見える構成を目指す。